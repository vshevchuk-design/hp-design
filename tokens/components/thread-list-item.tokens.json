{
  "component": {
    "threadListItem": {
      "$description": "The sixth piece of the family — the left-rail clickable row (subject + 2-column Department/Received-style metadata + chevron + a persistent selected accent), the actual list Message Center's thread pane is built from. Built on Card's own `interactive` shell per the approved plan — hover/pressed/focused colors are the exact same semantic values Card's interactive variant already resolved to (fill.primary border on hover, bg.primary+fill.primary on pressed, border.focus ring), verified at build time against Card's real resolved values (not retyped, and not cross-referenced at the token level either — this project's components never alias a sibling component's own tokens directly, only resolve-and-compare in the build script, same discipline as every other reuse in this family). 'selected' reuses the identical pressed pair, applied persistently rather than only on click — a selected row is functionally 'as if permanently pressed'. Where the live desktop app shows an expiry as bare red-colored text ('Expires: 03/01/2027'), this component upgrades that specific value to a real Badge (danger role) instead of inventing a second ad-hoc red-text convention now that a proper component exists for exactly this. No unread indicator (dot/bold subject) — not asked for, deferred rather than built speculatively.",
      "radius": { "$type": "dimension", "$value": "{radius.default}" },
      "padding": { "$type": "dimension", "$value": "{dim.4}" },
      "gap": { "$type": "dimension", "$value": "{dim.2}" },
      "bg": { "$type": "color", "$value": "{surface.default}" },
      "border": { "$type": "color", "$value": "{border.default}" },
      "divider": { "$type": "color", "$value": "{border.default}" },
      "subject": { "$type": "typography", "$value": "{text-style.heading-base}" },
      "subjectColor": { "$type": "color", "$value": "{text.default}" },
      "chevron": {
        "iconSize": { "$type": "dimension", "$value": "{dim.4}" },
        "color": { "$type": "color", "$value": "{icon.muted}" }
      },
      "metadata": {
        "$description": "Label:value rows, same line, spread apart (label left, value right) — matches the live app's own Department/Received/Regarding/Expires rows exactly. Both label and value at size.xs — secondary information, deliberately smaller than the subject; differentiated by color (muted vs. default), not weight.",
        "gap": { "$type": "dimension", "$value": "{dim.1}" },
        "label": {
          "$type": "typography",
          "$value": {
            "fontFamily": "{family.sans}",
            "fontWeight": "{weight.normal}",
            "fontSize": "{size.xs}",
            "lineHeight": "{leading.tight}",
            "letterSpacing": "{tracking.normal}"
          }
        },
        "labelColor": { "$type": "color", "$value": "{text.muted}" },
        "value": {
          "$type": "typography",
          "$value": {
            "fontFamily": "{family.sans}",
            "fontWeight": "{weight.normal}",
            "fontSize": "{size.xs}",
            "lineHeight": "{leading.tight}",
            "letterSpacing": "{tracking.normal}"
          }
        },
        "valueColor": { "$type": "color", "$value": "{text.default}" }
      },
      "inbox": {
        "$description": "Second content shape for the same interactive row — the Message Center inbox-list layout (avatar + department identity + timestamp + subject + preview + flag + optional Expires), as opposed to the Department/Received metadata-row shape above. Same shell, same hover/pressed/selected/focused (component.threadListItem.state) — only what's inside differs. Avatar is a real Avatar (resolved from avatar.tokens.json, base size), keyed off the department name (not a person) — same fallback-color hashing logic, just fed a department string instead of a person's name, so 'Academic Advising' always lands on the same identity color. No 'Handled by' row and no right-aligned Expires column (both present in the live inbox screenshot) — deliberately dropped/repositioned per explicit request: Expires moves to a left-aligned row directly under the preview text instead.",
        "avatarGap": { "$type": "dimension", "$value": "{dim.3}" },
        "lineGap": { "$type": "dimension", "$value": "{dim.1}" },
        "identity": {
          "$type": "typography",
          "$value": {
            "fontFamily": "{family.sans}",
            "fontWeight": "{weight.semibold}",
            "fontSize": "{size.sm}",
            "lineHeight": "{leading.tight}",
            "letterSpacing": "{tracking.normal}"
          }
        },
        "identityColor": { "$type": "color", "$value": "{text.default}" },
        "time": {
          "$type": "typography",
          "$value": {
            "fontFamily": "{family.sans}",
            "fontWeight": "{weight.normal}",
            "fontSize": "{size.xs}",
            "lineHeight": "{leading.tight}",
            "letterSpacing": "{tracking.normal}"
          }
        },
        "timeColor": { "$type": "color", "$value": "{text.muted}" },
        "preview": { "$type": "typography", "$value": "{text-style.body-sm}" },
        "previewColor": { "$type": "color", "$value": "{text.muted}" },
        "flag": {
          "iconSize": { "$type": "dimension", "$value": "{dim.4}" },
          "color": { "$type": "color", "$value": "{icon.muted}" }
        }
      },
      "state": {
        "hover": {
          "border": { "$type": "color", "$value": "{fill.primary}" }
        },
        "pressed": {
          "bg": { "$type": "color", "$value": "{bg.primary}" },
          "border": { "$type": "color", "$value": "{fill.primary}" }
        },
        "selected": {
          "$description": "The exact same pair as 'pressed', applied persistently (the row is currently open in the reading pane) rather than only for the moment of a click.",
          "bg": { "$type": "color", "$value": "{bg.primary}" },
          "border": { "$type": "color", "$value": "{fill.primary}" }
        },
        "focused": {
          "ringColor": { "$type": "color", "$value": "{border.focus}" },
          "ringWidth": { "$type": "dimension", "$value": "{dim.1}" },
          "ringOffset": { "$type": "dimension", "$value": "{dim.0_5}" }
        }
      }
    }
  }
}
