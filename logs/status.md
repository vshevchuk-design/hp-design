# Status — maintained snapshot (not append-only)

Update this file whenever component inventory or conventions change. For chronological history and the reasoning behind each decision, see `decision-log.md` — this file is the fast-load summary, that one is the archive.

Last updated: 2026-07-21.

## Components built (12)

| Component | File | Sizes | Variants | States |
|---|---|---|---|---|
| Button | `button.tokens.json` / `button.html` | sm 32 / base 40 / lg 48 | primary (filled blue) · secondary (filled gray) · ghost (transparent) — each ×5 content variants (text / icon-left / icon-only / icon-both / +counter) | default/hover/pressed/focused/disabled |
| Counter | `counter.tokens.json` / `counter.html` | sm 16 / base 20 / lg 24 | onPrimary (sits on Button primary) · onNeutral (sits on Button secondary/ghost, Tabs) | inactive/active |
| Input | `input.tokens.json` / `input.html` | sm 32 (no floating label) / base 40 / lg 48 (both float label to 12px) | icon-left optional | default/hover/focus/populated/disabled/error |
| Select | `select.tokens.json` / `select.html` | same grid as Input | icon-left optional; always has trailing chevron | same as Input. **Closed trigger only — no open menu/listbox built.** |
| Search | `search.tokens.json` / `search.html` | sm 32 / base 40 / lg 48 | leading search icon always; trailing clear (×) shown only when populated | default/hover/focus/populated/disabled (no error — no validation concept) |
| Pagination | `pagination.tokens.json` / `pagination.html` | one size, 32px items | page-number row w/ real ellipsis-collapsing algorithm (first/last always visible, no jump-to-first/last buttons needed) + prev/next arrows; rows-per-page reuses Select's `sm` directly | default/hover/active(current page)/disabled. No hard page-count limit — `overflow-x:auto` fallback. |
| Separator | `separator.tokens.json` / `separator.html` | one thickness, 1px | horizontal / vertical / with-label (center, 8px gaps) | **none — purely presentational, researched against Radix/shadcn/MUI/Mantine first** |
| Tabs | `tabs.tokens.json` / `tabs.html` | sm 32 / base 40 (no lg) | segmented (pill track) · underline (bottom bar) — each ×5 content variants (same as Button minus icon-both... actually: text/icon-left/counter/icon+counter/icon-only) | default/hover/active(selected)/disabled. No hard tab-count limit. |
| Checkbox | `checkbox.tokens.json` / `checkbox.html` | 1 size, 20px box | Native `<input>` visually hidden, custom box painted via `:checked`/`:disabled`/`:focus-visible` on the real input. Checked fills solid (fill.primary); indeterminate uses same fill, dash glyph. Hover (unchecked) is **fill.primary** border, not gray `border.strong` (too weak a cue). CheckboxGroup: vertical/horizontal, optional heading+helper, no error state. | default/hover/focused/checked/checked+hover/indeterminate/disabled/disabled+checked |
| Radio | `radio.tokens.json` / `radio.html` | 1 size, 20px circle, 8px dot | Same native-input approach as Checkbox. Checked never fills the circle — only outline + dot recolor, both **fill.primary**. Hover (unchecked) also fill.primary border, same reasoning as Checkbox. RadioGroup same shape as CheckboxGroup, items share one `name`. | default/hover/focused/checked/checked+hover/disabled/disabled+checked |
| Box | `box.tokens.json` / `box.html` | n/a | Lowest-level layout primitive — exposes surface (page/default/sunken/overlay)/border(on-off)/padding(none/xs/sm/md/lg — 0/8/12/16/24) directly, no fixed combination. Radius isn't re-declared — the full existing primitive radius scale applies as-is. Card is built on top of this, not a re-export of it. | none — purely presentational, like Separator |
| Card | `card.tokens.json` / `card.html` | n/a | One fixed combination for content grouping: surface.default + border.default + radius.default + dim.4 padding — **border-based, no shadow/elevation token** (none exists in this system; see convention note below). Optional header/footer sections divided by border.default. Optional `interactive` variant is a real `<button>`; hover/pressed border **fill.primary** (blue.500) + pressed fill **bg.primary** (blue.100) — deliberately not Button-ghost's fill.neutralHover/Active (calibrated for icon/pill-sized areas, reads as a flat gray block over a whole card) nor border.primary alone (blue.200 border + gray fill read muddy, two color languages at once). | default/hover/pressed/focused (interactive variant only) |

Text sizing: every component except Tabs scales label font-size with control size (sm/base/lg). Tabs is flat 14px at every size, per explicit spec. Input/Select/Search value & placeholder text is always 16px regardless of size — Safari iOS auto-zoom threshold.

## Locked-in conventions (don't relitigate these without reason)

- **Fill+border on form fields** (Input/Select/Search): `surface.sunken` bg + `border.default` hairline, strengthening to `border.strong` on hover, `border.focus` (blue) on focus. Not a pure outline or pure fill — evidenced by those tokens' own `$description`s, written before any component existed.
- **Ghost/ghost-like hover progression**: transparent (rest) → `fill.neutralHover` (hover) → `fill.neutralActive` (press/active). Used identically by Button ghost, Pagination page-items, Tabs (both styles, including underline's hover background — text-color-only hover was tried and found too subtle).
- **Disabled usually keeps the same background as default**, only text/icon fade to `*.disabled` — because `fill.disabled` / `surface.disabled` / `fill.neutral` / `surface.sunken` are all literally `{color.gray.100}`. Confirmed intentional, not a bug, recurred across Button secondary, Input, Select, Search.
- **CSS custom properties**: every resolved color is `--tok-<dotted-path-kebab>` (e.g. `fill.primaryHover` → `--tok-fill-primary-hover`), generated by `tools/lib/css-vars.mjs`. The `--tok-` prefix is load-bearing — without it a token can collide with the docs chrome's own `--text-primary`/`--border`/`--accent` (happened once, turned page text blue).
- **BEM**: shared block class (`.btn`, `.input`, `.select`, `.search`, `.tab`, `.counter`, `.page-item`, `.separator`) + `--modifier` for size/variant/state + `__element` for icon/label sub-parts. Where two variants share 100% identical sizing (Button primary/secondary/ghost; Tabs segmented/underline), the size grid is asserted identical at build time (`primarySizes.forEach(...) throw if diverged`) rather than trusted blindly — DTCG can't alias a whole sub-tree so the JSON duplicates the values, the assertion is the safety net.
- **Reused sub-components must resolve real values, never retype color-role names by hand** — caught two real bugs (Tabs' counter, both the wrong token guessed and a missing icon-disabled override) exactly from skipping this.
- **No hard caps on repeating collections** (Pagination pages, Tabs count) — `overflow-x: auto` + a practical guideline in the legend, confirmed with the user as the preferred approach over a hard-coded limit.
- Every docs page prints the *exact* generated CSS string both live and as the "copy this" code sample — one source, can't drift. Icons in printed code samples are `<!-- icon: name --></svg>` placeholder comments, not full path data.
- Radius is `radius.default` (8px) everywhere unless there's a specific reason otherwise (e.g. Tabs' `radius.md` track with a deliberately nested `radius.default` pill). **Concentric-radius rule for when a rounded shape sits flush inside another rounded shape** (touching edges, like Tabs' pill inside its track): `inner radius = outer radius − padding between them`, or equivalently `outer = inner + padding` — otherwise the two curves don't share a center and the gap looks uneven/bulging even with identical padding on all sides. Only relevant when one rounded shape is actually nested directly inside another (researched 2026-07-21) — a plain Card sitting on the page isn't nested in anything rounded, so it just uses `radius.default` with no extra math. Will matter for Modal/Drawer panel-inside-overlay later.
- **No implicit "default" padding value on Box** — confirmed against Chakra/MUI's own Box (2026-07-21 research): neither gives Box an implicit padding, always an explicit token choice, because unlike Button's height (one genuinely privileged size, used ~90% of the time) there's no single "right" padding for a generic container — it's entirely contextual (a chip vs. a section). Box's `none/xs/sm/md/lg` padding steps stay equally-weighted on purpose, no step singled out as "the" default. (Card, being one fixed *opinionated* composition rather than a generic primitive, is correctly the one that does have one fixed padding — `dim.4`.)
- **Native `<input>` for Checkbox/Radio**, visually hidden (clip-rect technique, not `display:none`) — keyboard/forms/screen readers/`:indeterminate` all work for free, only the paired `<span>` box/circle is repainted via `~` sibling selectors off `:checked`/`:disabled`/`:focus-visible` on the real input. Checkmark/dash glyphs reuse the shared Material icon set (`check.svg`/`remove.svg`), not bespoke assets.
- **`border.primary`/`border.success`/`border.danger`/`border.warning` are pale 200-tint roles, not brand-strength** — `border.primary` (blue.200, `#b2d4fe`) is much lighter than `fill.primary` (blue.500, `#077fec`). Radio's checked state first used `border.primary` for the ring, pairing a washed-out ring with a saturated dot — looked broken, caught before shipping. Fixed to reuse `fill.primary` for both ring and dot (same pattern Checkbox's checked state already uses for bg+border). `border.danger` at Input/Select error *is* the pale tint used correctly (a hairline-weight error border, not an emphasis border) — don't assume every `border.*` role is interchangeable with its same-named `fill.*`/`icon.*` counterpart; check the resolved hex before reusing one for emphasis.
- **No shadow/elevation token family exists anywhere in this system** — every surface (Input/Select/Search fields, now Card) draws separation with a `border.default` hairline, never a drop shadow, matching the flat Linear/Stripe-Dashboard/Notion aesthetic `typography.tokens.json`'s own `$description` names as the explicit reference. Card was built border-only on purpose, not because shadow was forgotten — don't introduce a shadow primitive for one component without a real cross-component need (e.g. a floating/overlay component that genuinely needs to look "lifted" off the page, unlike a page-level content Card).
- **Prefer white/default surface + hairline borders over stacking multiple gray tint layers for hierarchy** (e.g. gray page → grayer card → white inner card). User's prior design habit was layered depth via progressively darker/lighter grays (sometimes 3 levels); explicit call **2026-07-21**: favor a cleaner look for this system, closer to Linear/Stripe/Notion, and lean on `border.default` (or spacing/radius) to separate sections *before* reaching for a second surface tint. `surface.sunken` stays reserved for genuinely recessed elements (Input/Select/Search fields) — don't reach for it as a general "nested container" background. This is a default lean, not an absolute ban: a deliberate gray-card-on-gray-page case is still fine when a screen genuinely needs that grouping — just shouldn't be the reflex choice for every new panel/section.
- **Always render every state-dependent glyph/element in the markup, hidden via CSS, rather than conditionally injecting it at build time** — Checkbox's check/indeterminate icons were first only inserted into the HTML when the *doc-build script* had that item pre-checked; any live checkbox a user actually clicked (e.g. in the CheckboxGroup demo) had no icon element in the DOM at all, so clicking it just turned the box solid blue with no tick inside. Fixed by always emitting both glyphs with `display:none`, shown only via `:checked`/`:indeterminate` on the real sibling input — the same "let the real cascade drive it" principle already used for background/border color. Check any future component with build-time-conditional markup for the same trap.
- **`border.focus` is `{color.blue.600}` (not 500), ring width is `{dim.1}` = 4px (not `dim.0_5`/2px) — system-wide, affects every component with a focus ring/border** (Button, Input, Select, Search, Checkbox, Radio, Tabs, Card). Changed **2026-07-21** while designing Card's interactive hover/pressed: once hover/pressed started using `fill.primary` (blue.500) for a stronger, more visible cue, focus needed to move up a step too so keyboard focus stays unambiguously the *strongest* state — mouse hover is the common case but focus is the accessibility-critical one (a keyboard/switch user has no other way to see where they are), so it must out-rank hover, not just differ from it. Don't reset this back to 500/2px without re-checking every component's ring, not just Card's.
- **Unchecked-hover on Checkbox/Radio is also `fill.primary` now, not `border.strong`** — same "gray reads as too weak a cue" lesson from Card, applied consistently (2026-07-21). And for the checked+hover combination specifically, reuse `fill.primary`→`fill.primaryHover` (blue.500→600) — the *exact same pair* Button primary already uses for its own fill→hover darkening — rather than inventing a new darkening step. General pattern worth remembering: when a control's "on"/filled state needs a hover-darken, check whether the semantic layer already has a `*Hover` sibling for that fill role before creating anything new (it usually does — `fill.primaryHover`/`successHover`/`dangerHover`/`warningHover`/`neutralHover` all already exist).
- **Shadow/elevation tokens: still correctly absent, but flagged for when floating UI gets built** — Select's open dropdown, Popover, Tooltip all *genuinely* float above arbitrary page content (unlike a page-level Card, which sits in normal flow) — a border alone doesn't read as "lifted off the page" the way it does for Radix/MUI/etc.'s own floating layers. User confirmed (2026-07-21) this is the right moment to introduce a shadow primitive — but only when one of those components actually gets built, not speculatively now.

## Known non-bugs (don't "fix" these again)

- Component doc pages don't `<link>` the real Sora font file (only `typography.html` does) — falls back to system sans-serif. Sometimes renders slightly slanted in the sandboxed preview browser specifically; not reproducible as a real CSS `font-style` issue (checked via computed styles). Pre-existing since Button was first built, consistent across all pages.
- `file://` preview in the Browser tool is flaky for this repo (outside the sandbox's recognized project root). Use a local HTTP server instead — see `CLAUDE.md`.

## Open / deferred (revisit only when a real need appears — don't build speculatively)

- Select's open dropdown/listbox — only the closed trigger exists.
- A standalone Counter variant for plain page surfaces (nav badges, list rows) — only `onPrimary`/`onNeutral` (both assume sitting on a button fill) exist.
- Context-aware z-index selection for floating UI (Popover/Tooltip/Select-menu need to know whether they're mounted on-page vs. inside a Drawer vs. Modal L1/L2) — tokens exist (`z-index.tokens.json`), the selection *mechanism* (e.g. React context per Drawer/Modal) doesn't. **2026-07-21 research note:** since this repo is a static tokens+docs generator, not a live app, recommend *not* building a React-context auto-detection mechanism here — instead use native `<dialog>` (Modal/Drawer: free focus-trap, Escape-to-close, `::backdrop`, top-layer, no z-index needed) and the native `popover` attribute/Popover API (Popover/Tooltip: free top-layer + outside-click dismiss). Keeps the existing z-index scale as a documented manual fallback for non-native cases, doesn't require solving the hard auto-nesting problem.
- Grid, Scroll Area, Sidebar — user explicitly deferred these ("забагато напхав") until a concrete need names which one first.
- A handful of custom icons from the original product audit (grade badges, color swatches, a "primary" badge, class-location pin) were never sourced — not in Material Icons, need custom design.
- Original color review feedback (blue too saturated, yellow/orange too dark, violet too light) — deliberately un-revisited until real UI mockups exist to judge against.
- Vercel Deployment Protection was gating public access to the live site; user was walked through the dashboard fix themselves — not verified from this side whether it's been applied.

## Next

Container/overlay taxonomy researched 2026-07-21 (Radix, Bootstrap, Carbon, Atlassian): **layout primitives** (Box✅/Card✅/Grid — no interactivity) vs. **overlay family** (Modal/Dialog/Drawer are functionally one primitive with different positioning; Popover is the non-modal counterpart; Tooltip is Popover's hover-only, non-interactive-content sibling) vs. **structural** (Scroll Area, Sidebar). User's explicit decisions for when overlays get built: keep Modal and Drawer as **separate** token files/build scripts (not merged into one "dialog" file with position variants), and see the z-index research note above for the native-`<dialog>`/Popover-API recommendation. Grid/Scroll Area/Sidebar explicitly deferred — ask before starting any of them, don't assume priority.
