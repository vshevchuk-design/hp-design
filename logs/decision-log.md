# hp-design — decision log

Running, append-only log of what happened and what was decided. No formal tiers yet (no hot-memory workspace, no separate wiki) — just a chronological record so nothing gets lost. Newest entries at the bottom.

## 2026-07-20

- Repo created empty on GitHub (`vshevchuk-design/hp-design`), cloned locally. Default local SSH key belonged to a different GitHub account (`budda-hustle`) with no access — generated a dedicated key and a `github-vshevchuk` SSH host alias so both identities coexist.
- Decided token architecture: DTCG-format JSON as the source of truth (`tokens/primitives/`), stack-agnostic — will transform to CSS vars / Tailwind / etc. per consumer later. Layering: primitives → semantic (not started) → components (not started).
- Generated color primitives programmatically in OKLCH instead of hand-picking hex, anchored to the existing Figma brand colors (primary blue `#1565C0` etc.) but with consistent hue/chroma across each ramp — the reason the old brand's darkened yellow read as "muddy" was chroma collapsing at low lightness; keeping chroma consistent avoids that.
- Result: `tokens/primitives/color.tokens.json` — 10 ramps (gray + blue, red, green, amber, orange, violet, magenta, teal, brown), steps 25→950. Rule baked in: **step 500 = brand/UI-safe (≥3:1 contrast)**, **step 600 = AA text-safe (≥4.5:1 contrast)**.
- `tokens/primitives/chart-series.tokens.json` — validated categorical/chart palette (8 hues, fixed order: blue, green, magenta, amber, teal, orange, violet, red), passed the `dataviz` skill's six-checks CVD-safety validator (worst adjacent CVD ΔE 8.3, normal-vision floor 20.7, all ≥3:1 on `#fcfcfb`).
- Decided: background/notification tints use solid light steps (e.g. `blue-100`), not opacity overlays — opacity was the root cause of the original Figma sub-colors clashing across different surfaces.
- Built `docs/` as a small storybook-style static site: `index.html` (overview + left nav) and `colors.html` (palette page). `tools/build-colors-doc.mjs` regenerates `colors.html` from the token JSON — never hand-edit hex in the generated doc.
- **Open / deferred:** color review feedback — blue reads too fresh/saturated, yellow and orange read too dark, violet too light. Decided to move forward with current values and revisit once real UI mockups exist, not tune blind now.
- Discussed a long-term "AI Design Operating System" workflow (staged pipeline, tiered memory, review sandbox, decision log, archive) as a north-star direction. Decision: adopt pieces lazily, only when a real repeated pain shows up — not build the full pipeline/command set upfront. This log is the first small piece of that (the "Log & Learn" step), deliberately without the hot-memory/wiki tiers yet.
- Hosting plan: deploy `docs/` on Vercel via GitHub auto-deploy. Not yet connected — needs the user to link the GitHub repo to a Vercel account themselves.
- Status at end of session: nothing committed to git yet — all files exist locally only.

## 2026-07-20 (cont.)

- Added `tokens/primitives/dimension.tokens.json`, `radius.tokens.json`, `z-index.tokens.json` from the user's existing Figma scales. Dimensions matched Tailwind's spacing scale value-for-value (linear 4px grid) — kept as-is. Renamed the sentinel `dim-100` (999px, breaks the N*4 formula) to `dim-max` to match how radius already referenced it.
- Reviewed the z-index scale's ordering: initially flagged the "floating on page" tier (dropdown/popover/tooltip, 100-120) as a bug for sitting below Drawer/Modal tiers. User's counter-example (a page-level dropdown must not visually leak through a modal opened afterward) showed that a single globally-promoted floating tier would trade one bug for a worse one. Re-read the original design: it already has the right shape — `z-drawer-floating` / `z-modal-floating` / `z-modal-floating-2` exist precisely so a Select/Popover/Tooltip picks the slot matching whatever context (page / drawer / modal-1 / modal-2) it's currently mounted inside, rather than carrying one fixed z-index. Kept all values exactly as designed; documented the usage contract in each token's `$description` instead of renumbering anything.
- Open follow-up (component layer, not tokens): the context-aware z-index selection needs an actual mechanism when Select/Popover/Tooltip get built — e.g. a React context each Drawer/Modal provides to descendants so they know which "-floating" token to use. Not needed until those components exist.

## 2026-07-20 (cont. 2) — typography

- Added `tokens/primitives/typography.tokens.json` (family/weight/size/leading/tracking) and `text-styles.tokens.json` (17 composite DTCG `typography` styles) from the user's Figma spec. Font: Sora — confirmed English-only product, so Sora's lack of a Cyrillic subset on Google Fonts (checked directly against the API) is not a blocker.
- Size scale matches Tailwind's font-size scale value-for-value; `base` is deliberately 14px (not Tailwind/browser's 16px convention) — an app-density choice, same pattern as Linear/Stripe Dashboard/Notion, not an error.
- Two fixes applied vs. the literal Figma spec, both confirmed with the user first: `body-lg` was labeled with size `md` (16px) in the spec table, duplicating `body-md` and breaking the naming pattern every other row follows (`heading-lg`→`lg`, etc.) — fixed to `lg` (18px). `title-xl/2xl/3xl` used `leading-normal` (140%) like everything else, but at 20–30px bold that reads as too much air for a single-line title — switched those three to the already-existing `leading-tight` (120%), matching how Stripe/Linear/Vercel tighten leading as size grows.
- Added `tracking-wide` (0.04em, new — nothing in the source defined a non-zero tracking) and applied it to `label-sm`/`label-base`, which are all-caps and were at 0 tracking — all-caps at zero tracking reads cramped, near-universal fix.
- Downloaded Sora as 3 static woff2 weights (400/600/700, not the variable font — matches the 3-weight token scale exactly) into `assets/fonts/sora/`, plus a `sora.css` with `@font-face` rules.
- Extended the docs storybook: `docs/typography.html` (generated by `tools/build-typography-doc.mjs`, same pattern as colors), nav updated on all three pages so Typography is a real link, not "soon".

## 2026-07-20 (cont. 3) — layout page + docs legends

- Added `docs/layout.html` (dimensions + radius + z-index, generated by `tools/build-layout-doc.mjs`) and a "Legend/usage" section on every token docs page (colors, typography, layout) explaining where each token is meant to be used, not just its raw value — standing rule going forward, saved to memory.
- Fixed a real ordering bug: `Object.entries()`/JSON always sorts integer-like string keys ("0","1",…"96") numerically ahead of non-integer-like keys ("0_5","max"), regardless of insertion order — this is baked into the raw `dimension.tokens.json` file itself, not just the doc rendering. Fixed by sorting the doc table explicitly by resolved px value rather than trusting object key order; left the source JSON as-is since key order there is not semantically meaningful (DTCG consumers look up by name, not position).
- Corrected the dimensions legend after user feedback: 8px (`dim-2`) is not a "tight/border" value, it's the single most-used base gap in the whole scale (default spacing between cards, buttons, form fields) — recalibrated all the bucket boundaries around that.
- Z-index context column was wrapping to multiple lines — added short one-line context summaries (kept the fuller reasoning in the token JSON's own `$description`) plus an `overflow-x: auto` wrapper as a safety net.
- Radius preview boxes were too small (28px) to show the difference between md/lg/xl/full, which all looked like full circles — enlarged to 72px and removed the artificial px clamp, letting CSS's own border-radius clamping do the work authentically.

## 2026-07-20 (cont. 4) — semantic color tokens

- Decided to define core semantic color roles now rather than waiting for components: bg/text/icon/border/status/fill roles are universal (don't depend on which component gets built first), and the project's own stated rule ("build strictly on tokens") needs them to exist before the first component, or that component would have to hard-code primitives and get refactored later. Narrower, component-specific tokens (e.g. a chip's selected-state color) stay deferred until that component exists.
- Added `tokens/semantic/color.tokens.json` — 7 role groups (surface, bg, border, text, icon, fill, status), aliasing `tokens/primitives/color.tokens.json` via DTCG `{color.family.step}` refs, never duplicating hex. Modeled on the same bg-vs-fill split Anthropic's CDS token system uses (pale tint for passive status surfaces + text-role pairing, vs saturated solid for interactive controls + on-fill pairing) — referenced as a good pattern earlier in the project.
- Applied the primitives' own 500-vs-600 rule consistently: icon.* role colors use each family's 500 step (WCAG 3:1, graphics), text.* role colors use 600 (WCAG 4.5:1, small text) — same rule, correctly split by which one actually applies.
- Computed (not assumed) on-fill text contrast: checked white text and dark text (against the real text.primary ink, not literal black) on all four 500-step fills. Neither option reaches 4.5:1 on any of them — all land 3.7–4.8:1 regardless of choice. Conclusion: fill.* labels must be semibold/bold (relying on the WCAG large-text/UI-component 3:1 relief), documented directly on `text.onFill`, not left as a silent trap. Chose white uniformly across all four roles (more conventional, avoids an inconsistent per-role light/dark split for a marginal difference).
- `fill.success/danger/warning` alias `status.success/danger/warning` rather than duplicating the primitive ref — encodes that a fill is colored red *because* red means danger, not as a coincidence.
- `surface.overlay` is the one deliberate opacity value in the whole color system (`rgba(21,25,30,0.5)`, modal/drawer backdrop) — everything else uses solid tints per the earlier opacity-vs-tints decision, since a backdrop must composite over arbitrary page content while bg.* tints sit on a known surface.
- Added `docs/semantic-colors.html` (`tools/build-semantic-colors-doc.mjs`), resolving multi-level aliases (including alias-of-alias, e.g. fill.danger → status.danger → color.red.500) for display. First pass duplicated each group's description (a hardcoded legend string in the build script alongside the token's own `$description`) — fixed by adding `$description` to every semantic group in the token file and rendering only that, removing the hardcoded copy, so there's one source of truth for the legend text.
- User feedback: the doc page only showed resolved hex, not which primitive it aliases — the token file itself was always alias-based (`{color.gray.25}`, never raw hex), but that wasn't visible in the UI. Added an alias label to every swatch card. Also: surfaces read as "too gray" — `surface.page` was `gray.50`, one step more tinted than `surface.default` (`gray.25`), so the page canvas never actually read as the near-white the brand wants. Changed `surface.page` to `gray.25` too (same value as `surface.default`, on purpose) — cards now separate from the page via `border.default` (a hairline), not a background-color shift, matching the flat Stripe/Linear look the brief asked for. `surface.sunken` (`gray.100`) stays reserved for genuinely recessed elements only (inputs, code blocks, table stripes) — the exception, not the default.
- Bigger follow-up catch from the user: no literal white or black existed in primitives at all — black was fine (gray-950 already reads as near-black text ink), but white was a real gap. Separately, they noticed the gray ramp carried a deliberate blue tint (same hue as primary blue, low chroma) and, once surfaces leaned on gray-25 for the dominant background, that tint became visible enough to make surfaces look accidentally blue-branded.
- Fixed both: regenerated the gray ramp with chroma 0 (fully achromatic — `#fafafa`, `#f5f5f5`, `#808080`, etc, no hue at all) so neutrals never imply a brand color; added `color.white` (`#ffffff`) as its own literal primitive, explicitly not derived from the gray ramp (gray-25 is near-white but isn't identical to it). `surface.page`/`surface.default` and `text.onFill`/`icon.onFill` now alias `{color.white}` instead of `gray.25` or a hardcoded literal string — single source of truth. Added an "Absolutes" section to `docs/colors.html` so `white` is visible on the primitives page itself, not just implicitly through semantic tokens.

## 2026-07-20 (cont. 5) — icons

- User shared a screenshot inventory of icons already used across the current product (grouped: Navigation, Action, Status, Map, Content, Hardware, Audio & video, Communication), with a mix of exact and approximate/friendly names, and asked to pull the matching icons from google/material-design-icons — Filled style, SVG (not the font), downloaded rather than linked live from Google.
- The classic repo is enormous (multiple styles × PNG densities × font files); pulling `src` alone is still large, so instead fetched the repo's git tree via the GitHub API scoped to the `src` subtree (not the whole-repo recursive tree, which truncates before reaching `src`), giving the full list of ~2170 available "Filled" 24px icon names without downloading anything yet.
- Fuzzy-matched the user's ~110 requested labels against that real name list (exact match first, `difflib` close-match fallback, manual resolution for ambiguous ones) rather than guessing slugs from memory — caught real mismatches this way (e.g. "favorite_outline" → actual name is `favorite_border`; "download" arrow-into-tray → `get_app`; "Right"/"Left" turn arrows → `turn_right`/`turn_left`).
- A handful of requested items aren't extractable from Material Icons at all and were dropped rather than force-mapped: Grade A/B/C/F letter badges, plain color swatches ("Rect_green"/"Rect_purple"), a "primary" badge, and the "Class location" pin+graduation-cap icon — all clearly custom/product-specific glyphs, not from any icon set. Flagged for separate custom design work later.
- Added 27 additional common UI icons not in the user's list but expected to be needed soon (check, star/star_border, content_copy, refresh, cloud_upload, share, logout/login, sort, grid_view/view_list, dark_mode/light_mode, error_outline, check_circle, add_circle, remove, calendar_today, schedule, mail, visibility_off, delete/delete_outline, chevron_left/right, expand_more/less) — all verified to exist in the real name list before adding.
- Downloaded 136 SVGs total to `assets/icons/material-filled/{slug}.svg` (parallel curl, `raw.githubusercontent.com` — no GitHub API rate limit hit since only one API call was needed for the tree listing). Post-processed every file to add `fill="currentColor"` on the root `<svg>` (the source files ship with no fill on the glyph path, defaulting to black) so icons inherit ink color and work in both light/dark mode.
- Added `docs/icons.html` (`tools/build-icons-doc.mjs`) — icons embedded as inline SVG (required for `currentColor` to work; an `<img src>` reference would opt out of CSS color inheritance), grouped into the same categories as the user's source inventory, with a client-side name search/filter. Per the token-docs-legend standing rule, every icon shows a "not yet used" placeholder tag — to be rewritten to real component references as consumers appear, not before.
- This was a partial, on-demand extraction (136 of ~2170available), not a full import — matches the same "adopt lazily, add more as real need appears" pattern used throughout the project. Re-run the same tree-fetch + fuzzy-match approach next time more icons are needed rather than re-guessing slugs from memory.
- Two fixes from user feedback right after: (1) icons were previewed using the docs site's own chrome color variable (`--text-primary`, cosmetic styling for the storybook UI itself), not the design system's actual `icon.default` semantic token — inconsistent with every other page, which always renders real resolved token values. Fixed by resolving `icon.default` from `tokens/semantic/color.tokens.json` at build time and using that hex directly. (2) The category grouping borrowed from the user's source screenshot (Navigation/Action/Status/Map/…) read as arbitrary/confusing once rendered — dropped entirely in favor of one flat alphabetical grid with the search filter doing the organizing work instead.
- Reordered nav to Overview → Colors → Semantic colors → Typography → Layout → Icons → Components (semantic colors moved right after primitives colors); fixed a bug found along the way where `colors.html`'s own nav was missing the Layout link entirely.
- First git commit and push: `docs/`, `tokens/`, `assets/` (fonts + 136 icons), `tools/`, `logs/decision-log.md` — 161 files, pushed to `main` on `vshevchuk-design/hp-design`.
- Deployed `docs/` to Vercel (Root Directory: `docs`, framework preset: Other, no build step) — connected to the GitHub repo, auto-deploys on push from here on.
- Design reference noted from Vercel's own post-deploy success screen: white card on a gray page (same surface.default-on-surface.page hierarchy we just built), a solid black full-width primary button (deliberately neutral/restrained instead of accent-colored — worth remembering as an option, not a switch), and icon-in-a-soft-circle list rows for the "next steps" items — a clean pattern to reuse once the component library starts.
- Bug found on the live deploy: `typography.html`'s font `<link>` (`../assets/fonts/sora/sora.css`) 404'd in production — Vercel's Root Directory was set to `docs/`, so `assets/` at the repo root (a sibling of `docs/`, not inside it) was outside the deployed tree entirely. First fix attempt mirrored `assets/` into `docs/assets/` (`tools/sync-docs-assets.mjs`) — worked, but the user immediately flagged it as confusing (two copies of every font/icon, unclear which was canonical). Reverted that mirror. Correct fix is on the Vercel side: set **Root Directory back to the repo root** (blank/default) instead of `docs` — restores a true single source of truth for `assets/`, at the cost of the site living under a `/docs/` URL prefix rather than the bare domain root. Repo side already reverted and pushed (`../assets/...` link restored, mirror deleted); the Root Directory change itself has to happen in the Vercel dashboard, which only the user can do.
- Separate deploy blocker hit when the user tried the Root Directory fix: Vercel rejected the deploy — "commit author did not have contributing access to the project on Vercel... Hobby Plan does not support collaboration for private repositories." All 3 commits so far were authored as `vs-make-value <vs.make.value@gmail.com>` — a third identity, inherited from this machine's pre-existing *global* git config, unrelated to either GitHub account used this session (`budda-hustle` or `vshevchuk-design`). SSH key controls push access; commit author identity is a separate `user.name`/`user.email` field that had never been set for this repo specifically. User can't easily produce a verified email for `vshevchuk-design` (SSO restrictions on that account block them from even running Claude Code as that identity), so used GitHub's account-ID-based noreply address instead — `300203481+vshevchuk-design@users.noreply.github.com` (ID looked up via the public GitHub API) — which GitHub always attributes correctly to that account regardless of what real email is on file. Set via **repo-local** `git config` (not `--global`, to avoid touching the pre-existing identity for the user's other projects). Rewrote all 3 existing commits' authorship with `git rebase --root --exec "git commit --amend --no-edit --reset-author"`, then force-pushed since the original (wrongly-authored) commits were already on the remote.

## 2026-07-20 (cont. 6) — first component: Button (primary) + Counter (onFill)

- First entry in the component layer (`tokens/components/`), built strictly on the semantic layer per the project's own standing rule — zero hardcoded values, every leaf is a DTCG alias back through semantic → primitives.
- Added `icon.disabled` to `tokens/semantic/color.tokens.json` (`gray.300`, mirrors `text.disabled`) — a real gap found while building the disabled button state: every other icon role (default/secondary/muted/onFill/accent/success/danger/warning) existed, disabled didn't.
- `tokens/components/button.tokens.json` — `component.button.primary`, 3 sizes (sm 32 / base 40 / lg 48) × 5 content variants (text-only, icon-left+text, icon-only square, icon-left+text+icon-right, icon-left+text+counter-right) × 5 states (default/hover/pressed/focused/disabled).
  - User's spec gave flat numbers (24px icon, 12px edge padding, 8px icon↔label gap) without saying whether they hold across all 3 sizes or describe the base size only. Icon size: resolved the user's own follow-up question (20 for base? 16 for sm?) by picking 16/20/24 across sm/base/lg — lands on exactly 50% of button height at every step, so it's a formula, not per-size guessing, and keeps working if a 4th size gets added later. paddingX: scaled 8/12/16 (dim-2/3/4) rather than a flat 12, driven by the same height-step logic and also required by "icon-only is square" — a flat 12px edge pad on a 32px sm button leaves no way to hit a 32×32 square with a 16px icon. Flagged this deviation from the literal spec to the user rather than silently deciding.
  - Gap (icon↔label) stays flat 8px at every size, exactly as specified — no scaling reason applies to it the way it does to padding/icon size.
  - Radius: `radius.default` (8px), constant across all 3 sizes — sizes change height/padding/icon/label, not the corner.
  - Label typography reuses existing `text-style.heading-sm/base/md` (semibold, no new style invented) rather than adding a dedicated button text-style — matches the project's "build strictly on tokens" bias toward reuse.
  - Focus ring: additive 2px ring (`border.focus`) at 2px offset, layered on top of whichever fill state is already showing (default/hover/pressed) rather than replacing it — keyboard focus and mouse hover/press aren't mutually exclusive.
- `tokens/components/counter.tokens.json` — `component.counter.onFill`, the one variant actually needed right now (sitting inside `button.primary`'s icon+text+counter-right variant). Deliberately did *not* build a general-purpose standalone counter for plain surfaces (nav badges, list rows) — no real use case for it yet, matches the project's lazy-adoption pattern used throughout.
  - Sizes mirror button.primary's sm/base/lg 1:1, height pinned to the same dim step as that size's iconSize (16/20/24px) so the pill's edge lines up with the icon beside it. Numeric label composed directly from typography primitives (bold weight, `leading.none` for tight vertical centering) rather than aliasing an existing `text-style.label-*` — those bake in uppercase + wide tracking for all-caps words, wrong for digits.
  - Two states: inactive = `fill.accentActive` bg + `text.onFill` label (quiet, close to the button's own color) — deliberately not `fill.accentHover`, which would make the counter visually vanish the moment the parent button is hovered. Active = inverted to white bg + `text.accent` label, so a new/meaningful count pops instead of blending in.
- Docs: added `tools/build-components-doc.mjs` → `docs/components.html`, same generated-from-tokens pattern as every other docs page (a real alias-resolving walker, not hand-copied values) — renders live buttons/counters styled directly from resolved token hex/px, a size×variant matrix, a states matrix (default/hover/pressed/focused/disabled, real CSS pseudo-classes plus a static reference swatch row), and a legend section per the standing "explain where each token is meant to be used" rule. Per the user's request, added an explicit placeholder note that the preview icons (add/arrow_forward) are stand-ins only — the real icon per real button gets decided when an actual use case is composed, same convention as the icons page's own "not yet used" tags.
- Flipped `Components` from a disabled "soon" nav placeholder to a real link across all 6 docs pages (`docs/index.html` hand-edited; the other 5 are generated, so the nav block was fixed in each of their `tools/build-*-doc.mjs` sources and regenerated, not hand-patched in the output HTML).
- User feedback on first look: Button — no changes. Counter had two issues. (1) The doc preview rendered each counter sitting on a blue box styled to look like the real button fill, which read as "is this an actual button?" rather than a swatch — fixed by replacing it with a neutral gray, non-rounded, fixed-56×56 demo container (same size regardless of which counter size sits inside), plus an explicit note that it's a demo box, not a real button, matching how `layout.html`'s radius preview already uses a fixed-size neutral box for the same reason. (2) User flagged that Counter will also be needed inside a future secondary button and, "most likely," standalone near the header — a real signal that the `onFill`-only scope decided earlier might be too narrow. Asked directly rather than guessing: kept 3 sizes (confirmed, still synced to button.primary's sm/base/lg). On the neutral/gray variant for secondary-button + standalone use — user chose to wait until those components actually get built rather than build it speculatively now, consistent with the project's lazy-adoption pattern; `component.counter` stays `onFill`-only for now.
- Restructured the sidebar nav into two labeled categories now that Components exists: **Tokens** (Colors / Semantic colors / Typography / Layout / Icons) and **Components** (Button / Counter, as two separate items instead of one "Components" link) — Overview stays outside both categories. Button/Counter link to `components.html#button` / `#counter` (added matching `id` attributes to those section headings); both stay `active` while on that page since there's no scroll-spy JS, matching this site's static-generated approach throughout. Applied identically across all 7 pages — `docs/index.html` hand-edited, the other 6 fixed in their `tools/build-*-doc.mjs` sources and regenerated.
- User immediately caught the consequence of "no scroll-spy": Button and Counter lit up active at the same time regardless of which one was clicked, reading as broken rather than intentional. Fixed properly instead of leaving it — added a small `IntersectionObserver` scroll-spy script to `docs/components.html` only (the one page that needs it; every other docs page still has zero client JS, that convention stands elsewhere). Server-rendered HTML now defaults Button to active (the first/top section) so there's no flash-of-both-active before the script runs; the script then keeps whichever section is actually scrolled into view highlighted.
- Bigger restructure, prompted by the user: `components.html` (Button + Counter as sections on one page) was reading as everything "blending together," and — separately — the user wants real Storybook-style pages: one page per component, with actual code shown for each example, not just a rendered mockup.
  - Split into `docs/button.html` and `docs/counter.html`, generated by new `tools/build-button-doc.mjs` / `tools/build-counter-doc.mjs`. Deleted `docs/components.html` and `tools/build-components-doc.mjs` — fully superseded, not kept around.
  - Each page now generates one real, valid, copy-pasteable CSS stylesheet (`.btn-primary` / `.counter` base class + `--sm/--base/--lg` size modifiers + state pseudo-classes/modifiers) from the resolved tokens, and — critically — that exact same generated string is what's injected into the page's own `<style>` tag AND what's printed in the "CSS" code block. Same for each example's HTML markup: the literal string used to render the live preview is what gets escaped and printed underneath it. One source for preview and code, so they cannot drift apart, which a hand-typed-alongside-the-real-styles approach would risk (caught and fixed exactly this kind of drift once already: the button page's first draft had the `<style>` tag re-typed by hand instead of reusing the generated CSS string, and the focused-state code snippet referenced a `var(--page-bg)` that doesn't exist anywhere in the stylesheet — both fixed to reuse the one real source).
  - Icons in printed code samples are shown as `<svg class="btn-primary__icon"><!-- icon: add --></svg>` (a short placeholder comment) rather than the full raw path-data SVG dump — the live preview still renders the real inlined icon for visual accuracy, only the *printed* sample is abbreviated, since a wall of path data isn't useful as a copyable snippet.
  - Much bigger type scale and spacing than the old page: h1 22px→36px, section headings 12px-uppercase→24px-bold with a top border and ~90px of top margin between major sections (CSS / Sizes / Content variants / States for Button; CSS / Sizes / States for Counter) — the exact "everything blends together" complaint.
  - Button's three axes (size, content variant, state) got their own sections instead of one giant cross-product grid — 3+5+5 = 13 story cards instead of attempting 3×5×5. Sizes section uses the icon-left+text variant as reference (documented that size and content variant are independent, any variant works at any size); Content variants section fixes size at base; States section fixes size+variant at base+icon-left-text. Counter mirrors this with just its two axes (size, state) — 3+2 = 5 cards.
  - Nav had grown into 9 near-identical hand/generated copies of the same sidebar block (a direct pain point from the last change, which required touching 7 files for one link edit) — extracted to `tools/lib/nav.mjs` (`NAV_ITEMS` config + `renderNav(activeKey)`), imported by all 6 generator scripts. `docs/index.html` stays hand-authored (pre-existing, deliberate exception) but its nav markup is kept in sync by eye against the same source of truth.
  - Overview page's single "Components" card split into separate "Button" and "Counter" cards, matching the sidebar's new two-item Components category.

## 2026-07-20 (cont. 7) — token-purity audit + accent→primary rename

- User reacted to seeing literal hex in the printed "CSS" code sample on button.html ("бачу кольори хардкодом, незадоволений") and asked for a full audit that everything is strictly token-driven, not just colors that happened to catch the eye.
- Fix 1 (the immediate complaint): colors in the generated `.btn-primary`/`.counter` stylesheets were resolved to literal hex at build time — correct values, but frozen, not live-bound to the token. Added `tools/lib/css-vars.mjs` (`cssVarName` path→CSS-var-name convention, `renderRootVars`) — both `button.html` and `counter.html` now emit a `:root { --fill-primary: #077fec; ... }` block generated from the same resolved tokens, and every color usage in the stylesheet is `var(--x)`, never a literal hex.
- Did a systematic audit before declaring it done, not just fixing the one thing pointed at: walked every leaf value in `tokens/components/button.tokens.json` and `counter.tokens.json` (100% aliases, zero raw values — clean) and `tokens/semantic/color.tokens.json` (one deliberate exception, `surface.overlay`'s rgba, already documented from the original semantic-color session). Then grepped the two build scripts' generated CSS specifically (not the docs-site chrome, a different category — see below) for anything not routed through `resolve()`/`cv()`. Found two real misses: (1) `font-family: 'Sora', sans-serif` was hand-typed instead of pulled from `family.sans` — fixed, now `var(--family-sans)`, itself in the same `:root` block. (2) The focus ring's box-shadow referenced `var(--bg-card)` — not a design token at all, it's this docs site's own card-background chrome variable, presented inside "copy this code" as if it were part of the system. Rather than force a wrong fix (the ring inherently needs to know its local background, which is contextual — hardcoding it to `--color-white` would look right in the docs' light mode and wrong in dark mode), added an explicit inline comment (`/* substitute your own surface color */`) printed right in the code sample so nobody copies `--bg-card` expecting it to exist elsewhere.
- Explicitly scoped what did *not* get token-ified, and said so out loud rather than silently: (a) dimensions (height/padding/gap/font-size/radius) stay literal px derived from resolved tokens, not re-exposed as CSS custom properties — a deliberate distinction between "themeable at runtime" (colors, fonts) and "a baked layout recipe per size" (spacing), the same distinction most real component libraries draw; (b) the docs site's own chrome (`--bg-page`, `--border`, `--accent`, nav/card styling, present on all 7 pages since the very first one built) is a separate meta-palette for the documentation tooling itself, never claimed to consume the product's own design tokens — same relationship as Storybook not being built out of your own design system.
- Separately, user pushed back on the semantic color role name itself: `fill.accent`/`bg.accent`/`text.accent`/`icon.accent` are literally the one dominant brand blue, used for the *biggest, most prominent* surfaces (a full Primary button fill) — not what "accent" conventionally means in design terminology (a small, secondary highlight color, distinct from the main brand color). Agreed this was a real naming smell, not just a style preference.
- Considered renaming to `brand` (avoids any collision with `component.button.primary`) vs `primary` (reuses the word already used for the button's emphasis level — same convention Bootstrap/Chakra/etc. use, since the primary button IS the one that gets the primary/brand color, so the reuse is coherent rather than confusing) — user chose `primary`.
- Hit a real blocker executing that: `text.primary` already existed with a completely different, unrelated meaning (`gray.950`, the default body/heading ink — every page's base text color). Renaming `text.accent` → `text.primary` would have silently collided two unrelated concepts under one key. Flagged it before touching anything rather than guessing; user chose to rename the *existing* `text.primary` (ink) → `text.default` first, freeing up `text.primary` for the blue-text role. `bg`/`border`/`icon`/`fill` had no such collision.
- Executed: `tokens/semantic/color.tokens.json` — `text.primary`(ink)→`text.default`; `bg.accent`→`bg.primary`, `border.accent`→`border.primary`, `text.accent`→`text.primary`(new meaning), `icon.accent`→`icon.primary`, `fill.accent/accentHover/accentActive`→`fill.primary/primaryHover/primaryActive`. Updated every `$description` that referenced the old names (3 spots mentioning "never with text.primary" meant the ink role, corrected to "text.default"). Propagated through `tokens/components/button.tokens.json`, `counter.tokens.json`, `tools/build-button-doc.mjs`, `tools/build-counter-doc.mjs`, and a stray prose mention in `tools/build-icons-doc.mjs`'s legend. `tools/build-semantic-colors-doc.mjs` needed no code change at all — it renders semantic groups by iterating `Object.entries()` dynamically rather than hardcoding key names, so the rename flowed through automatically, confirming that page was already built the right way. Verified zero remaining references to the old names anywhere in `tokens/` or `tools/` (docs-site chrome's own unrelated `--accent`/`--accent-bg` CSS vars deliberately left alone — different category, per the point above). Regenerated and re-validated all 7 docs pages.

## 2026-07-20 (cont. 8) — Button: secondary variant

- Added `component.button.secondary` to `tokens/components/button.tokens.json`, same size/state/content-variant grid as primary. User asked gray-fill vs outline — answered from evidence already in the codebase: `fill.neutral`'s own `$description` (written back in the original semantic-color session, before any component existed) literally says "Secondary/neutral button fill" — the intended direction was already decided, not a fresh 50/50 call. Went with gray fill.
- Color mapping: default → `fill.neutral` bg, `text.default` label, `icon.default` icon. hover → `fill.neutralHover`. pressed → `fill.neutralActive`. disabled/focused reuse the same universal `fill.disabled`/`text.disabled`/`icon.disabled`/`border.focus` tokens as primary (deliberately shared, not duplicated per-variant).
- Caught and documented a real consequence before it became a surprise: `fill.disabled` and `fill.neutral` are both `{color.gray.100}` — identical value. A disabled secondary button therefore doesn't change background at all, only the label/icon fade to `text.disabled`/`icon.disabled`. Flagged to the user as a heads-up (not a blocker) rather than silently shipping a state that looks like it "does nothing."
- Also caught during implementation, not by inspection after the fact: primary's label/icon happen to share one color (`text.onFill` == `icon.onFill`, both white) so the original CSS got away with a single `color` property inherited by the SVG's `currentColor`. Secondary's `text.default` (gray.950) and `icon.default` (gray.700) are genuinely different values — relying on inheritance would have been silently wrong. Gave `.btn__icon` its own explicit color rule per variant instead (retrofitted onto primary too, so both variants work the same correct way rather than primary "working by coincidence").
- Refactored `tools/build-button-doc.mjs` from a single-variant script into a proper shared-base structure now that two variants genuinely share identical sizing: introduced a BEM block `.btn` (structural: display/border/cursor/font-family) + shared `.btn--sm/--base/--lg`/`--icon-only` size modifiers + `.btn__icon` element, with `.btn--primary`/`.btn--secondary` as the only variant-specific (color) modifiers — mirrors Bootstrap's own `btn btn-primary btn-sm` convention. Added a build-time assertion comparing `button.primary.size` against `button.secondary.size` (resolved values, deep-equality) that throws if they ever diverge — guards against the two JSON blocks silently drifting apart, since DTCG can't alias a whole sub-tree so the size values are duplicated in the source file by necessity.
- Page restructured to fit two variants: added a `Primary`/`Secondary` heading tier (`h2.big-section`) above the existing `Sizes`/`Content variants`/`States` tier (demoted to `h3.mid-section`) — one shared `CSS` section up top covering both variants' rules in one stylesheet, since it genuinely is one stylesheet.
- Left open, not built now: the icon+text+counter content variant on Secondary currently reuses `counter.onFill` as-is (tuned for the blue primary fill) sitting on a gray button — untested against the "no speculative variants" rule in either direction (reusing existing state is the lazy/correct default per that rule), but visually it may read as an odd blue-on-gray badge. Called out directly in the page copy so it doesn't look like an oversight; revisit if/when it looks wrong in practice or a neutral counter variant becomes independently necessary.

## 2026-07-20 (cont. 9) — two real bugs from the secondary button build

User reported two problems immediately after building Secondary: body text on button.html/counter.html was rendering blue instead of black (but fine on every other page), and the secondary button's counter only ever appeared to show one state, never a visibly "quiet" one.

- **Root cause 1 — CSS custom property name collision.** Every docs page's own chrome CSS (nav, cards, reading text — unrelated to the design system) declares a `:root` with `--text-primary` meaning "default near-black body text." `tools/lib/css-vars.mjs`'s `cssVarName()` mapped the *renamed* semantic token `text.primary` (the brand-blue text role, from the accent→primary rename two sessions ago) to the exact same CSS variable name, `--text-primary`. button.html and counter.html are the only two pages that emit a component `:root` block — and because it appears later in the same `<style>` tag, it silently won the cascade over the chrome's own declaration, turning all page text blue. Same-named `:root` in two unrelated places, one clobbering the other — never caught because the token-purity audit checked for *hardcoded* colors, not for a *naming* collision between the token layer and the docs tooling's own chrome.
  - Fixed structurally, not by picking a different one-off name: every token-derived CSS variable is now prefixed `--tok-` (`cssVarName()` in `tools/lib/css-vars.mjs`), so a design-token var can never again collide with whatever the docs chrome happens to call its own reading-UI colors, regardless of what either naming scheme does in the future.
- **Root cause 2 — Counter's one "inactive" treatment doesn't work on both button colors.** `counter.onFill.inactive` (dark navy bg + white label) was tuned to blend into a *blue* button. Secondary's button fill (`fill.neutral`, gray.100, nearly white) is a completely different family — the same dark-navy pill reads as the *loudest* thing on the button instead of a quiet one, which is why the user could only find what looked like an "active/attention" treatment and never found anything that read as quiet.
  - Restructured `tokens/components/counter.tokens.json`: hoisted `size`/`radius` to the top of `component.counter` (genuinely shared, no reason to duplicate them the way button's per-variant `size` had to be), and replaced the single `onFill` variant with two surface variants — `onPrimary` (for button.primary's blue fill, unchanged colors) and `onNeutral` (for button.secondary's gray fill: inactive = `fill.neutralActive` bg + `text.default` label, mirroring onPrimary's "one step past hover" logic in the gray family; active = inverts to `fill.primary` blue, not white — gray.100 is already near-white so a white "active" pill would barely show, the same problem in reverse).
  - `tools/build-button-doc.mjs`'s counter markup now picks `counter--onPrimary` or `counter--onNeutral` based on which button variant is rendering (`counterSurfaceFor` map), instead of always hardcoding one surface regardless of context — that hardcoding was the actual bug; the color mismatch was just its visible symptom.
  - `tools/build-counter-doc.mjs` restructured to document both surfaces as parallel sections (mirroring how button.html documents Primary/Secondary), rather than one flat Sizes/States pair.
- Regenerated and spot-checked all 7 docs pages; confirmed only the docs-chrome's own (legitimate, unchanged) `--text-primary` declarations remain anywhere, and that Secondary's counter story now resolves to `.counter--onNeutral.counter--inactive`, distinct from Primary's `.counter--onPrimary.counter--inactive`.

## 2026-07-20 (cont. 10) — Input, Select, Search

Three new form-field components in one push: `tokens/components/input.tokens.json` / `select.tokens.json` / `search.tokens.json`, each with its own `tools/build-*-doc.mjs` → `docs/*.html`, same sm(32)/base(40)/lg(48) size grid as Button.

- **Fill vs outline** — user asked directly, same as the Button secondary-variant question. Answer was already sitting in the token descriptions from the original semantic-color session, before any component existed: `surface.sunken`'s own `$description` says "Inputs...recessed" and `border.default`/`border.strong` say "input borders at rest"/"input border on hover" verbatim. Not a 50/50 call — went with both together (sunken fill + hairline border), the standard modern pattern (Stripe/Linear/Notion), not a pure pick-one.
- **Floating-label spacing math didn't check out.** User had hand-derived padding (2px top/bottom at base, 4px top/bottom + 4px label-value gap at lg) from 12px/16px text at 140% line-height. Verified it: both target sums came out to a consistent 36px (internally consistent, good sign), but real 140% line-height gives label+value = 39.2px, a genuine 3.2px overshoot in both cases — not a rounding error, a real mismatch. Rather than force exact pixel padding that only works if line-height rounds a specific way (fragile — Safari/Chrome round fractional line-heights differently), implemented via flexbox: fixed height, `display:flex; flex-direction:column; justify-content:center;`, explicit `gap` (0 at base, 4px at lg per the token). Same visual result, immune to the line-height rounding disagreement entirely. Also switched label/value line-height to `leading.tight` (1.2) rather than the reading-paragraph `leading.normal` (1.4) — form-field text, not body copy.
- **Floating label has two distinct layouts, not one animating element**: resting (empty + unfocused — single centered 16px placeholder line, structurally identical to sm's only layout) vs floated (focused or populated — 12px label stacked above the 16px value). Since every story card is a static snapshot rather than one interactive widget, each renders the correct layout directly rather than faking a CSS-only `:placeholder-shown` transform trick — simpler, and nothing here needed live animation to make its point.
- **States**: default / hover / focus / populated / disabled / error (Input, Select) — `populated` named deliberately instead of the more common "filled", to avoid colliding with the unrelated `fill.*` token family (a button's `fill.primary` and an input's "filled" state would read as related when they aren't). Focus turns the floated label `text.primary` (blue) — reverts to `text.muted` once blurred even if still populated, so blue always means "the active field", never "has a value". Recurring pattern flagged again (third time this session, now clearly established as a real property of this token set, not a one-off surprise): `surface.disabled` and `surface.sunken` are both `{color.gray.100}` — disabled doesn't change the field's background, only text/icon/label fade.
- **Search deliberately diverges from Input/Select**: no floating label at any size (placeholder just vanishes on focus, sm/base/lg alike — simpler than Input because there's no label to coordinate with) and no error state (a search box has no validation concept the way a form field does). Leading search icon always present; trailing clear (×) icon renders only once there's a value.
- **Select scoped to the closed trigger only** — same "document what's real, defer the rest" pattern as Counter's onFill/onNeutral split and the earlier z-index floating-UI deferral: the open menu/listbox isn't built (no options list, no floating-UI z-index wiring), called out explicitly in the token's own `$description` rather than left as a silent gap. Always renders a trailing chevron (`expand_more`), unlike the optional icon-left — not a content variant, every select has one, pushed to the trailing edge via `margin-left: auto`.
- Extended `tools/lib/nav.mjs` and `docs/index.html` (nav + 3 new cards) for all three; regenerated all 10 docs pages clean.
- User feedback pass: Button/Select's cursor already felt right (`.btn`/`.select` both explicitly set `cursor: pointer`) but Input/Search felt off — the cursor only changed to an I-beam directly over the rendered placeholder/value glyphs, not across the rest of the box. Real `<input>` elements get `cursor: text` over their whole padded box natively; since Input/Search are `<div>`-based static mockups (not real `<input>` elements — a deliberate choice so every state could be shown as a reliable static story card instead of relying on live focus/typing), that native behavior never got set explicitly, so the browser fell back to only showing an I-beam where text-selection heuristics kick in (over actual glyphs) and a default arrow everywhere else in the box. Fixed by adding `cursor: text` to `.input`/`.search`'s base rule and `cursor: not-allowed` to their `--disabled` modifier (the same pairing `.select`/`.btn` already had) — not a token-driven value, a plain structural CSS declaration, same category as `border: none`/`display: flex` elsewhere in these stylesheets.

## 2026-07-20 (cont. 11) — Pagination

Added `tokens/components/pagination.tokens.json` + `tools/build-pagination-doc.mjs` → `docs/pagination.html`.

- User shared 4 references: the current ad-hoc implementation (double `<<`/`>>` jump-to-first/last arrows + a numeric stepper, called "стрьомно"/sketchy), their own earlier sketch (`1 2 [3] 4 5 … 12` with a rows-per-page dropdown), and two shadcn examples. Kept the current version's *capability* (jump straight to the first or last page) but not its *implementation* (extra arrow buttons) — first and last page are now always rendered as real, clickable numbers, never collapsed behind the ellipsis, so clicking the visible `1` or the visible last number does the same job the removed `<<`/`>>` buttons did, with one fewer control to build/style.
- Active-page treatment: solid `fill.primary` bg + `text.onFill` label, matching the user's own sketch rather than shadcn's outline-box treatment — reuses the primary button's exact fill, not a new color.
- Implemented the range-collapsing logic as a real algorithm (current ± 1 sibling, pin first/last, collapse the rest into one ellipsis per side — the standard shadcn/Mantine-style pagination range function), not a handful of hardcoded example strings. Verified its output at 4 different current-page positions (near start, middle, near end, and short-enough-to-not-collapse-at-all) rather than assuming it was right.
- Single size only (32px items) — unlike button/input/select/search's sm/base/lg, pagination is a compact utility control for tables/lists, not something that needs an emphasis ladder. Not asked by the user this time (unlike every other component this session, where sizes were explicit) — read as a signal this component doesn't need them, mentioned the decision rather than silently deciding or blocking on a question.
- Rows-per-page isn't a new dropdown style — `build-pagination-doc.mjs` loads `select.tokens.json` directly and renders its `sm` size, so it stays pixel-identical to the real Select component as that one evolves, instead of a hand-styled lookalike that could drift.
- Hover fill reuses `fill.neutralHover` (secondary button's own hover fill) rather than a new token — same "subtle gray on hover" visual intent, no reason to duplicate it.
- Extended nav + index cards; regenerated all 11 docs pages, verified zero literal hex outside the printed `:root` blocks and the page-range algorithm's output matched hand-computed expectations exactly at all 4 tested positions.

## 2026-07-20 (cont. 12) — Pagination full bar

User caught a real gap: rows-per-page and the page-range nav were documented as two separate pieces, but in real usage (a table/list footer, per the shadcn reference) they always appear together in one row. Added `component.pagination.bar` (a single `gap` token, dim.6/24px — the minimum breathing room between the two groups when the container isn't wide enough for `justify-content: space-between` to matter on its own) and a new "Full bar" section at the top of `docs/pagination.html`, rendered full-width (outside the normal `story-grid`, which would have squeezed it into a 320px column) so it actually reads like a table footer rather than another small card.

## 2026-07-20 (cont. 13) — Separator

User asked to research common practice first, not guess. Searched Radix/shadcn/MUI/Mantine's own Separator/Divider components before building `tokens/components/separator.tokens.json` + `tools/build-separator-doc.mjs` → `docs/separator.html`.

- Confirmed by research: horizontal + vertical orientation is standard across every reference; none of them give the divider interactive states (it's purely presentational) — so this is the first component this session with no default/hover/focus/disabled section at all, and the legend says so explicitly rather than silently omitting a section a reader might expect.
- Color: `border.default` — its own `$description` already said "...card edges, dividers, input borders at rest" back in the original semantic-color session, before any component existed. Third or fourth time a token's own description has directly answered a component question before it was even asked.
- Thickness: 1px, kept as a literal (not a token) — checked, no design system scales hairline width the way it scales spacing/typography.
- With-label variant per the user's spec exactly: line – 8px gap (`dim.2`) – label – 8px gap – line, all centered via flexbox. Label color: `text.secondary` (AA-safe), not `text.muted` — a divider label ("3 replies", "Today") is often meaningful content, not pure decoration, so it shouldn't default to the below-AA-contrast role.
- Extended nav + index card; regenerated all 12 docs pages, verified zero literal hex outside the printed `:root` block.

Sources consulted: [Separator – Radix Primitives](https://www.radix-ui.com/primitives/docs/components/separator), [Separator - shadcn/ui](https://ui.shadcn.com/docs/components/radix/separator), [Divider - Material UI](https://mui.com/material-ui/react-divider/), [Divider - Mantine](https://mantine.dev/core/divider/).

## 2026-07-20 (cont. 14) — Tabs

Added `tokens/components/tabs.tokens.json` + `tools/build-tabs-doc.mjs` → `docs/tabs.html`.

- Two styles per the user's own two reference screenshots: **segmented** (a track, active tab raised on a white pill) and **underline** (plain text, active tab marked by a 2px bottom bar). Two sizes only (sm 32 / base 40, no lg) — a two-size control, matching what was actually asked rather than defaulting to the button's 3-tier scale everywhere.
- Clean semantic-token reuse, found rather than invented: segmented's track uses `surface.sunken` (the same "recessed" token an input's own background already uses) and the active pill uses `surface.default` — literally the same sunken/default pairing cards already use for "raised above the page." `pillRadius` (8px) is deliberately `trackRadius` (12px) − `trackPadding` (4px) so the active pill's corners nest correctly inside the track's rounded corner, not a coincidence.
- Text stays a flat 14px at both sizes per the user's explicit spec (unlike button/input, where type scales with control size) — only icon size, padding, and the counter scale between sm/base.
- Counter variant reuses `component.counter.onNeutral` exactly as built for the secondary button (inactive tab → quiet gray, active tab → pops blue) rather than inventing new counter colors — same visual distinction, repurposed from "notification freshness" to "tab selection," which the tokens don't encode either way (they only define appearance, not why).
- **Caught and fixed two real bugs while resolving the reused counter, before shipping**: (1) the CSS generation had hand-retyped the counter's color roles instead of resolving them from `counter.onNeutral`'s own token values — and the hand-typed guess was wrong (`fill.neutralHover` instead of the actual `fill.neutralActive`), exactly the kind of drift risk resolving-from-source is supposed to prevent. Fixed by reading the real `$value` path strings off `counter.onNeutral.state.*` and feeding those into `cv()`, so it can't happen again silently. (2) The tab icon had one fixed color (`icon.default`) with no `disabled` override, so a disabled tab's icon stayed full-strength while its label faded to `text.disabled` — the same icon/label decoupling bug caught and fixed on Button/Secondary and Input earlier this session, this time caught before commit rather than after user feedback.
- Tab count: no hard limit enforced by the component (a content decision, not a token) — confirmed with the user first rather than assumed. `overflow-x: auto` on both style containers as the fallback, plus a practical (5-7 comfortable) guideline in the legend. Demonstrated with a real 8-tab overflow example in a 480px container rather than just asserting it works.
- Extended nav + index card; regenerated all 13 docs pages, verified zero literal hex outside the printed `:root` blocks.

## 2026-07-20 (cont. 15) — Tabs States section had no visible background

User caught it immediately: the segmented States examples rendered as plain text floating on white, states indistinguishable. Root cause: `.tab` itself has no background — only its `.tabs--segmented` parent (the track) carries `surface.sunken`. The "Styles"/"Sizes"/"Content variants" sections all correctly render full `tabBar()` output (tab wrapped in its track), but "States" rendered a bare `tabItem()` with no wrapper — the one section that skipped it. Fixed by wrapping each state example in the same `.tabs.tabs--segmented.tabs--base` container the other sections already use, in both the live preview and the printed code sample (so the copyable snippet is accurate too, not just the preview).

## 2026-07-20 (cont. 16) — Tabs: hover was invisible on both styles

User caught two more real issues on the same page.

- **Segmented default/hover looked identical.** Root cause: a CSS specificity tie resolved by source order, not intent. `.tab--hover-demo` (a single class, defined early in the docs-chrome part of `<style>`) and the real component's `.tab` base rule (also a single class, defined later inside the generated `${css}` block) both have equal specificity — CSS breaks ties by "whichever rule comes last in the source wins," and `.tab` came after `.tab--hover-demo`, so the real base styles silently overrode the demo override on every property they both touched (background, color). Fixed by making the demo class a compound selector, `.tab.tab--hover-demo` — two classes beats one regardless of source order, so it can't lose that tie again.
- **Underline hover (text-color-only) was too subtle to read**, per the second screenshot. Asked what "the pros" do: checked against GitHub/Notion/Linear's own tab hover — all of them give hover a light background pill even in underline-style tab lists, reserving the colored bar exclusively for the active tab. Added exactly that: `.tabs--underline .tab:hover` now gets `fill.neutralHover` (the same token segmented's own hover already uses — no new color) plus a top-rounded corner (`radius.default`, matching the pill shape) so it doesn't look like a stray square edge butting into the underline baseline.
- Added a parallel "States — underline" section (previously states were only demonstrated for segmented) so the fixed hover is verifiable as a static screenshot too, not just by moving a mouse over the live preview.

## 2026-07-20 (cont. 17) — Button: Ghost variant

Added `component.button.ghost` — third, quietest tier below primary/secondary.

- No fill or border at rest — not a new pattern: reuses the exact transparent → fill.neutralHover → fill.neutralActive progression already established by pagination's page-item and tabs' segmented style earlier this session. First time a button variant's `default` state has no `fill` token at all — handled in `tools/build-button-doc.mjs` by treating a `fill: null` marker as a literal `"transparent"` in the generated CSS, both for the resting state and for disabled (which also stays transparent — ghost never had a background to lose, unlike primary/secondary where disabled swaps in `fill.disabled`).
- Label/icon: `text.secondary`/`icon.secondary` at rest — deliberately one step quieter than secondary's `text.default`/`icon.default`, since ghost sits a rung below secondary in the emphasis ladder and should read softer, not identical.
- Counter variant pairs with `counter.onNeutral`, same as secondary — both live in the same neutral/gray visual family, no reason for a third counter surface.
- Extended the build-time size-identity assertion (previously primary-vs-secondary only) to check all three variants against primary's grid in one loop.
- Generalized two spots that had hardcoded `key === "secondary"` conditionals (the counter-surface link text, the disabled-background note) to read from `counterSurfaceFor[key]` / add a ghost-specific branch instead of just secondary's — otherwise ghost's copy would have silently fallen back to the wrong (onPrimary) counter reference.
- Regenerated all 13 docs pages; verified zero literal hex outside the printed `:root` blocks and that the build's own identical-size assertion passed for ghost without needing to loosen it.

## 2026-07-21 — CLAUDE.md + status.md: the "hot-memory" tier finally earned its keep

User asked for a session-handoff digest, worried about losing context between sessions. This is exactly the pain point flagged as a maybe-later item in the very first entry of this log ("adopt pieces lazily, only when a real repeated pain shows up... this log is the first small piece, deliberately without the hot-memory/wiki tiers yet") — the pain showed up, so the next piece got added, not before.

- Added `CLAUDE.md` at the repo root — auto-loaded by Claude Code at the start of every session in this directory, no reminder needed. Kept deliberately short (it's loaded into every session's context whether needed or not): what the project is, the layering rule, the component build pattern, the load-bearing conventions most likely to get silently violated by a fresh session, and the file:// preview workaround. Points to `logs/status.md` as the first real read.
- Added `logs/status.md` — a *maintained* (edited in place, not appended) snapshot: component inventory table, locked-in conventions, known non-bugs (so they don't get "fixed" again), open/deferred items, and a "next" pointer. This log (`decision-log.md`) stays exactly what it always was — append-only chronological history with the *why* behind each call — status.md is the fast-load summary sitting on top of it, not a replacement.
- Going forward: update `status.md` in place whenever the component inventory or a locked-in convention changes; keep writing normal dated entries here for anything that needs the fuller reasoning preserved.

## 2026-07-22 — Checkbox + Radio

Native `<input>` visually hidden (clip-rect technique, not `display:none`) under a custom-painted box/circle, styled entirely off `:checked`/`:disabled`/`:focus-visible` on the real input — keyboard/forms/screen-readers work for free, same idea Radix/MUI/Ant use. Checkbox box fills solid (`fill.primary`) when checked; indeterminate reuses the same fill with `remove.svg` instead of `check.svg`, set via a tiny inline script since `indeterminate` is a JS-only DOM property with no HTML attribute. Radio's checked state never fills the whole circle — only the outline + a small inner dot recolor — the universal convention that visually distinguishes it from Checkbox at a glance.

- Real bug caught and fixed: Checkbox's glyphs were only inserted into the markup when the *doc-build script* had that item pre-checked at generation time. Any live checkbox a user actually clicked (e.g. the CheckboxGroup demo) had no icon element in the DOM at all — clicking it just turned the box solid blue with no tick. Fixed by always rendering both glyphs with `display:none`, shown only via `:checked`/`:indeterminate` on the real input — the same "let the cascade drive it" principle already used for color.
- Real bug caught and fixed: Radio's checked state first used `border.primary` (blue.200, a pale tint role) for the ring, pairing a washed-out ring with a saturated `fill.primary` dot — looked broken. Fixed to `fill.primary` for both ring and dot, matching how Checkbox's own checked state already pairs bg+border.
- User feedback, applied consistently: unchecked-hover on both used `border.strong` (gray) first — read as too weak a cue in practice. Switched to `fill.primary` (the same brand blue used when checked) for hover on both controls.
- Follow-up feedback: hover should be a clear color shift, not just a half-shade-darker gray — confirmed `fill.primary` (blue.500) reads right. For the *checked*+hover combination specifically, reused the existing `fill.primary`→`fill.primaryHover` pair Button primary already uses for its own hover-darken, rather than inventing a new step.
- User pushed further: since `border.focus` (blue.500 at the time) and the new hover blue were now the same intensity, focus needed to out-rank hover for real, not just differ from it — a keyboard/switch user has no other way to see where they are, so focus must read as unambiguously the strongest state even though mouse hover is the common case. Resolved by moving `border.focus` to `blue.600` and the focus ring width from `dim.0_5` (2px) to `dim.1` (4px) — **system-wide**, affecting every component with a focus ring (Button, Input, Select, Search, Checkbox, Radio, Tabs, Card retroactively).

## 2026-07-22 (cont.) — Box + Card

Box: the lowest-level layout primitive — `surface` (page/default/sunken/overlay) × `border` (on/off) × `padding` (a curated 5-step scale) directly, no fixed combination, no states (purely presentational like Separator). Radius isn't re-declared — the existing primitive radius scale applies as-is. Card: one fixed, opinionated combination on top of Box for content grouping (`surface.default` + `border.default` + `radius.default` + `dim.4` padding), optional header/footer sections, optional `interactive` variant (a real `<button>`).

- User explicitly deferred Grid/Scroll Area/Sidebar this round ("забагато напхав") after a broader container/overlay taxonomy discussion — see the "overlay family" entry below for how that research paid off later the same session.
- Real design question raised and resolved: does Card need a shadow? No — audited the system and confirmed no shadow/elevation token exists anywhere; every surface (Input/Select/Search, now Card) draws separation with `border.default`, matching the flat Linear/Stripe-Dashboard/Notion aesthetic `typography.tokens.json`'s own `$description` already named as the reference. Didn't introduce one for a single non-floating component — flagged explicitly as the right call *until* a genuinely floating component needed it (paid off later — see overlay family).
- User feedback on prior design habit: previously favored layered depth via progressively darker/lighter grays (sometimes 3 levels stacked — page → card → inner card). Explicit call this session: favor a cleaner look closer to Linear/Stripe/Notion, lean on `border.default`/spacing/radius to separate sections *before* reaching for a second surface tint. Not an absolute ban — a deliberate gray-on-gray case is still fine when a screen genuinely needs that grouping, just shouldn't be the reflex default.
- Card's `interactive` hover/pressed states went through three iterations before landing: (1) `fill.neutralHover`/`fill.neutralActive` (Button-ghost's own progression) — read as a flat gray block over a whole card, since those tokens are calibrated for a small icon/pill area and the same hex reads far more intense over a big surface; (2) `border.primary` (pale blue.200) for hover, paired with a gray fill for pressed — the gray+blue mix looked muddy, two color languages at once; (3) final: `fill.primary` (blue.500, same brand blue Checkbox/Radio use checked) for both hover and pressed borders, `bg.primary` (blue.100, the same passive-tint role used for banners/badges) for the pressed-only fill — one consistent blue family. This is what forced the `border.focus`/ring-width bump described above, so focus would still clearly out-rank the new, bolder hover.
- Also gave Box's `padding.sm` a real value: added a 12px step (`dim.3`), which meant renaming the old 8px `sm` to `xs` to keep `md`(16)/`lg`(24) untouched — `none/xs/sm/md/lg` = 0/8/12/16/24.
- User asked why radius/padding don't have a "base" step like Button's `sm/base/lg`: confirmed via research (Chakra/MUI's own Box has no implicit default padding, on purpose — unlike Button's height, there's no single "right" padding for a generic container) that Box's steps should stay equally-weighted, no step singled out as default.

## 2026-07-22 (cont. 2) — Switch + Grid

Switch: same native-input-under-custom-visual pattern as Checkbox/Radio, but the track is **always a solid fill**, even off (`border.default`) — unlike Checkbox/Radio's hollow-until-checked shape, matching every reference (Radix/MUI/Ant/iOS). Checked/hover reuse the `fill.primary`/`fill.primaryHover` pair already established. No SwitchGroup — switches are typically standalone settings rows, not a shared-selection list.

Grid: `display:grid` + gap scale only (`none/xs/sm/md/lg`, the *exact same* steps/aliases as Box's padding — user's own suggestion, reused rather than inventing a second scale). Column count isn't tokenized — a structural per-use decision, same reasoning as Box not tokenizing radius. Demonstrated with fixed `repeat(N,1fr)` counts and an `auto-fit, minmax(…,1fr)` responsive variant — the same pattern this docs site's own `.story-grid` chrome already uses, no breakpoint tokens needed.

## 2026-07-22 (cont. 3) — overlay family: Tooltip, Popover, Drawer, Modal

User asked to build "the overlay family" components. Researched taxonomy first (Radix, Bootstrap, Carbon, Atlassian): layout primitives (Box/Card/Grid, no interactivity) vs. overlay family (Modal/Dialog/Drawer are functionally one primitive with different positioning; Popover is the non-modal counterpart; Tooltip is Popover's hover-only sibling) vs. structural (Scroll Area/Sidebar, deferred). Also revisited the long-deferred "context-aware z-index" problem from 2026-07-20 (cont. 1) — since this repo doesn't implement a live component tree, recommended native browser mechanisms over a hand-rolled React-context solution, which the user confirmed.

Built in order of complexity: Tooltip (pure CSS) → Popover (native `popover`/`popovertarget` attributes) → Drawer → Modal (both native `<dialog>`+`showModal()`). All four get focus-trap/Escape/outside-dismissal/top-layer stacking from the browser for free — no JS state machines, no z-index anywhere (including nested modals, which stack automatically via the browser's own top-layer order). This is also where `shadow.tokens.json` (3 steps: sm/md/lg) and semantic `surface.inverse` (Tooltip's dark bg) got introduced — the first and, so far, only components in the system that use either, deliberately scoped to floating UI (every other surface still uses a border).

First-pass review round (user looked at screenshots of all four) surfaced several real gaps, all fixed same session:
- Tooltip shipped with no arrow — read as ambiguously floating rather than clearly pointing at its trigger once seen live. Added a small rotated-square `::after`, same bg as the bubble. Lesson: some visual calls need to be seen live before they're trustworthy, not just reasoned about from references (the original "no arrow" call *was* checked against Linear/Vercel first, and still turned out wrong in this context).
- Popover's close (×) and Drawer/Modal's close + footer actions initially had zero interactive states, or used ad-hoc placeholder CSS (`.demo-trigger`) that didn't match the design system's real Button recipe at all. User: "чи хрестик — це частина візуалізації, чи компонента?" — answer: it's part of the real component, anything clickable in a demo needs real states. Fixed with the actual Button semantic recipe (`fill.neutral`/`fill.primary`/`fill.danger` + their hover/active siblings + `radius.default`), not a look-alike.
- Drawer/Modal's header/footer border-bottom/border-top first lived inside one shared-padding `.content` wrapper, so the divider stopped short of the panel's real edges — looked misaligned at that scale (fine on Card, which is small enough that an inset divider reads as intentional). Fixed by making header/body/footer each their own full-width band with its own padding.
- Only demonstrating the fully-composed (header+body+footer) version made the structure look mandatory. Added explicit body-only examples — same trap Card's own "Body only" story had already been built to avoid, forgotten when Drawer/Modal shipped.
- User caught the sharpest bug last: opened the body-only Drawer and found no way to close it. `showModal()` gives Escape-to-close for free but *not* click-outside — a separate, easy-to-miss `<dialog>` gotcha. Fixed with the standard MDN light-dismiss recipe (dialog's own padding is 0, `.content` fills 100% of it, so a click landing on the `<dialog>` element itself rather than a descendant can only mean it hit the backdrop).

## 2026-07-22 (cont. 4) — dropdown family: AlertDialog, Menu, Listbox

User wanted "the dropdown that drops from Selects and buttons" — could be text-only, text+icon, or text+checkboxes with select-all/clear-all and search for the Select case. First instinct was to propose one blended "Dropdown" component (simpler, since this repo doesn't implement real keyboard nav/ARIA anyway) — user corrected this directly: optimize for how real downstream developers will consume it (eventual Tailwind/React port), not for what's convenient to build here. Real component libraries (Radix etc.) split action-menus from selectable-option-lists because the ARIA roles and keyboard models genuinely differ (menu/menuitem vs. listbox/option) — saved as a standing feedback memory, not just a project note, since the principle generalizes beyond this repo.

Landed on three additions, all built on Popover's shell (native `popover` attribute, `border.default`+`shadow.sm`+`radius.default`):
- **AlertDialog** — a behavioral variant of Modal, not a new component: zero new tokens. A `data-alert` marker attribute makes the shared click-outside script skip it, and one extra listener blocks Escape via `preventDefault()` on `<dialog>`'s native `cancel` event. For confirmations critical enough that an accidental dismiss would be bad; no close (×), only explicit footer buttons dismiss it. Verified via real event dispatch that outside-click and the cancel event are both correctly blocked while the actual Cancel/Delete buttons still work.
- **Menu** — dropdown actions (Rename/Duplicate/Delete), text-only and icon+text variants, an optional divider, and a destructive item that hovers into `bg.danger` (pale red tint) instead of the usual `fill.neutralHover`, so it stays "red" rather than reverting to neutral gray. Items use `radius.xs`, concentric with the panel's `radius.default` minus its own padding — the same lesson learned building Tabs' pill-in-track much earlier this project, reapplied deliberately.
- **Listbox** — selectable options (what Select's own dropdown needs). Single-select shows a trailing checkmark (`fill.primary`) with no background tint, matching Radix Select / native `<select>` convention. Multi-select uses a real Checkbox marker per option — literally Checkbox's own tokens (`dim.5` box, `radius.xs`, `fill.primary` checked), not re-derived — plus an optional search field that reuses Search component's own sm-size tokens verbatim, and an optional select-all/clear-all row (multi-select only) where "Clear all" uses `text-style.link-sm`.
- Bug caught while wiring the link style: `resolveToken()` (the shared per-build-script helper that walks a token's `$value` tree) silently drops `$extensions` — the field `text-style.link-sm`/`label-sm`/etc. use for `textDecoration`/`textTransform` — since it only ever reads `.$value`. "Clear all" rendered with no underline with no error anywhere. Fixed by fetching `$extensions` directly from the referenced node instead of trusting the resolved typography object to carry it. Flagged (not yet checked) that `counter.tokens.json` also references `label-sm`/`label-base` and may have the same latent gap.
- Select's own open dropdown/listbox (deferred since 2026-07-20) is now unblocked — Listbox is exactly the panel it needs.

## 2026-07-22 (cont. 5) — session housekeeping

Caught a dating mistake partway through wrapping up: every "2026-07-21" written into `status.md` during this entire session (all the Checkbox/Radio-through-Listbox work above) should have said **2026-07-22** — copied forward from the file's pre-existing "Last updated: 2026-07-21" line at the very start of the session instead of using the actual current date, and never caught until doing a deliberate end-of-session review. Fixed with a global replace across `status.md` plus the one persistent-memory file written this session. Worth double-checking the actual current date against any inherited "last updated" text at the start of a session, rather than assuming it's still accurate.

## 2026-07-22 (cont. 6) — Avatar

User asked directly for Avatar (not picked from the still-open Badge/Tag/Toast/Avatar list) — "sm, md, lg sizes; photo if there's one, initials if there isn't, look at how the pros do it." Researched the fallback pattern first: Radix Avatar (`Image`/`Fallback`, onLoadingStatusChange), MUI Avatar (`onError` swap to initials), and GitHub's per-user deterministic identity color all converge on the same chain — image, then name-derived initials, then a generic icon if there's no name either. Built exactly that as `tokens/components/avatar.tokens.json` + `tools/build-avatar-doc.mjs` → `docs/avatar.html`.

- Sizes named `sm/base/lg` (not the literally-requested `md`) to stay consistent with every other sized component in this system (Button/Input/Select/Search all use `sm/base/lg`) — flagged as a deliberate naming call, not a misread of the ask. Values (32/40/48, `dim.8/10/12`) are the *exact same* grid as Button/Input/Select/Search on purpose, not an independently chosen scale, so an avatar lines up at the same height as a button or field beside it in a row (a user-menu trigger, a comment composer). `iconSize` (generic fallback) is 50% of diameter at every step — the same ratio Button's own `iconSize`/`height` already uses.
- Initials fallback needs a color, and picking one arbitrarily (or letting every fallback be the same gray) is a known weak spot in naive implementations — GitHub/Slack solve it by hashing the name into a small fixed palette so the same person always lands on the same color without storing one per user. Implemented that: `hash(name) % 8` selects one of 8 hues.
- Those 8 hues needed real token backing, not inline hex. Considered reusing `tokens/primitives/chart-series.tokens.json` directly (same 8-hue CVD-safe order already exists there) but rejected it — chart-series sits at the 500 step, tuned for saturated lines/bars on a plot area, wrong contrast/weight for a pastel fallback badge, and reusing it would blur a token that's explicitly documented as chart-only (its own `$description` already warns against exactly this kind of cross-use for `status`). Also considered the 4 existing semantic role colors (`bg.primary/success/danger/warning`) — rejected because coloring someone's avatar `bg.danger` would visually read as an error/alert state on that person, wrong meaning entirely, unrelated to why that color was picked. Added a new `avatar.*` semantic group instead (`tokens/semantic/color.tokens.json`) — 8 hues in the same order as chart-series (for one consistent categorical rhythm across the system) at bg@100/text@600 (matching the existing `text.primary/success/danger/warning` 600-step AA-safe convention), scoped explicitly to this one use.
- That group nests one level deeper than every prior semantic group (`avatar.hue.bg`/`avatar.hue.text` vs. a flat `name.key`) — `tools/build-semantic-colors-doc.mjs`'s `renderGroup()` assumed flat and would have thrown resolving it. Generalized it to walk down to whichever depth actually holds a `$value` (`leaves()` helper) instead of hardcoding one level — confirmed the existing flat groups (surface/bg/border/text/icon/fill/status) still render identically after the change, not just that avatar now works.
- Initials typography composed directly from primitives (`weight.semibold`, size scaled per step, `leading.none`, `tracking.normal`) rather than aliasing an existing `text-style.*` — same reasoning Counter's own label already established: the bold/label styles bake in uppercase+wide tracking meant for words, wrong for 1-2 bare letters. Used `semibold`, not `bold` — full bold read slightly heavy at this scale for just two letters.
- Every avatar (photo or fallback alike) gets a 1px `border.default` hairline — the same "separate with a border, not a shadow" convention Card/Input/Select/Search already established — because a light-toned photo or a pale fallback tint can otherwise blend straight into `surface.page`'s white with nothing marking its edge.
- Didn't build AvatarGroup (an overlapping stack) or a presence/status dot — both are genuinely separate compositions on top of a single Avatar, and neither was asked for; deferred explicitly rather than built speculatively, same discipline Switch applied skipping a group variant it wasn't asked for either.
- The doc page's "broken photo" story wires a **real** `<img>` with a deliberately invalid `src` and a genuine `error` event listener that reveals the sibling fallback (both elements always render, `hidden` toggles which shows — the same "let the real cascade drive it" approach already established for Checkbox's check/indeterminate glyphs) rather than a forced inline style standing in for a screenshot. Verified via direct DOM inspection after load that the listener actually fired (`img.hidden === true`, fallback visible, correct initials) rather than trusting it visually.
- Extended `tools/lib/nav.mjs` and hand-edited `docs/index.html` (nav link + overview card) per the standing wiring convention; regenerated all docs pages clean, verified `docs/semantic-colors.html` now lists the new `avatar` group correctly alongside the 7 pre-existing ones.

## 2026-07-22 (cont. 7) — language mistake: avatar.html shipped in Ukrainian

User caught it immediately after the Avatar build landed: the standing rule (stated in an earlier session, not written down anywhere in this repo's own files — a real gap) is chat happens in Ukrainian but every docs-site artifact is English. `avatar.html`'s legend/section prose/story titles/sample names were written entirely in Ukrainian, breaking that rule outright — worse than a one-off slip, since it's the first component page in the whole project written that way (every prior page's legend/notes were already in English, confirmed by grepping for Cyrillic across `docs/*.html`).

- Rewrote every user-facing string in `tools/build-avatar-doc.mjs` (legend rows, section headings/descriptions, story titles/notes, the JS section's own commentary) to English, and swapped the Ukrainian sample names (`Марія Іваненко` etc.) for English ones — picked via the same deterministic `hash(name) % 8` script used to build the original palette story, re-run against English first/last-name lists so all 8 hues are still each represented by a distinct name (`Michael Wolfe`/`Daniel Kerr`/`Emma Hayes`/`Laura Chen`/`David Grant`/`Olivia Lynn`/`James Walker`/`Sarah Bennett`), not by coincidence.
- Grepping for Cyrillic across `docs/*.html` turned up two more, pre-existing violations of the same rule, from earlier in the project — not caused by this session's work but real gaps worth fixing while already in here rather than leaving a known issue behind: `docs/index.html` (the overview page's lede + every card's `<p>` blurb, going back to whichever component was on top when each one was written) and one line in `tools/build-colors-doc.mjs`. Translated both to English.
- Separately noticed every single docs page (all 27, including ones with zero Cyrillic content) declares `<html lang="uk">` — an attribute that's been wrong since the very first page (`colors.html`, 2026-07-20) since the actual content has always been English; nobody had reason to notice until this Avatar mistake prompted an actual content-language audit. Fixed system-wide (`sed` across every `tools/build-*.mjs` plus `docs/index.html`, all `lang="uk"` → `lang="en"`) rather than only touching the 3 files with visible Ukrainian text, since the attribute itself was mislabeling every page regardless of its content.
- The standing chat-Ukrainian/docs-English rule was never actually written down anywhere a fresh session would see it (not in `CLAUDE.md`, not in `status.md`) — it lived only in prior-session conversation, which is exactly the kind of thing this project's `status.md`/`CLAUDE.md` pair exists to prevent from getting lost. Worth adding as an explicit line in `CLAUDE.md` so it isn't re-broken the next time a component gets built from a fresh session.
- Regenerated all 27 docs pages after the fixes; verified zero remaining Cyrillic characters anywhere under `docs/*.html` and `lang="en"` on every page.

## 2026-07-22 (cont. 8) — Avatar: gray border read muddy on colored fallbacks

User feedback after seeing the palette section live: the 1px `border.default` gray hairline (applied uniformly to every avatar per the original design) looked dirty sitting on top of the 8 saturated identity-color fallbacks specifically — user offered either "remove it" or "tint it to match the avatar's own color" as options.

- Picked removal over per-hue tinting: the border's whole reason for existing is solving a real contrast problem (a light photo or the near-white neutral fallback blending into `surface.page`'s white) — none of the 8 identity colors have that problem, their pastel bg already reads as a distinct block against a white page on its own. Tinting the border to match would have meant either 8 new border-color tokens (real token-surface bloat for a cosmetic-only fix) or reusing the same `avatar.hue.bg`/`text` refs for a third purpose they weren't designed for; removing it is simpler and addresses the actual root cause (an unnecessary border) rather than dressing it up.
- `tools/build-avatar-doc.mjs`: each `.avatar--{hue}` rule now also sets `border-color: transparent` — kept `border-width: 1px` (didn't drop to `border: none`) so the box-sizing math stays identical across every fallback variant, no accidental 1px sizing wobble between colored and non-colored avatars. Same "swap in the literal `transparent`, don't invent a token for 'off'" pattern this project already established for Button ghost's unfilled rest state.
- `tokens/components/avatar.tokens.json`'s `border.$description` updated to document the split (load-bearing on photo/generic fallback, intentionally overridden to transparent on the 8 identity-color fallbacks) rather than leaving the token's own docs silently out of sync with what the CSS actually does.
- Verified via computed styles, not just visually: `avatar--blue` etc. resolve to `border-color: rgba(0,0,0,0) / border-width: 1px` (transparent, not literally removed) while `avatar--neutral` and the live broken-photo demo's real photo state still resolve to the actual `border.default` gray — confirms the split lands exactly where intended, not an accidental blanket removal.

## 2026-07-22 (cont. 9) — Message/Bubble/Attachment family: planning + Badge

User surfaced 4 shadcn "AI Elements" primitives (`Bubble`/`Message`/`Attachment`/`MessageScroller`) as a possible reference for a whole new messaging-component family, driven by two real products: Message Center (currently live, student-facing, email-like) and an upcoming AI chat product. Asked explicitly for a **plan**, not code yet — researched first (fetched all 4 shadcn docs pages, searched Zendesk/Front/Help Scout/Cloudscape patterns for rich-content-in-a-conversation-thread precedent), then re-read all 5 provided Message Center screenshots (the live desktop student app) plus 7 staff-side mobile/desktop mockups frame-by-frame.

**The actual finding** (not just component-shopping): the rich-vs-simple split in the real product isn't sender-based (staff vs. student), it's **content-type-based**. Institution announcements (Winter Intersession, financial aid notices) render as a plain rich block — headings-as-bold-labels, mixed text sizes, bulleted links, images, CTA buttons, no bubble shape at all in the live desktop app. Any short conversational reply — including the *staff's own* quick replies ("Thanks for letting me know...") on the Requirement Waiver Request thread, not just the student's — renders as a compact tinted bubble. Confirmed this by checking that exact thread specifically, since it disproves the naive "staff=plain, student=bubble" assumption. A third, rarer content type showed up once (`Course No Longer Offered`): a literal embedded marketing HTML email in its own bordered chrome — flagged as an edge case, deferred rather than built.

- Entered plan mode, wrote the full plan to `/Users/mac/.claude/plans/distributed-sprouting-gadget.md` (kept outside the repo per this session's plan-mode tooling — re-read that file for the complete reasoning if resuming this family cold in a future session, `status.md`'s own entry only summarizes it).
- One real fork needed the user's call before finalizing: the live Message Center screenshots show rounded-**square** initials avatars (AR/BL/LA), directly contradicting last session's explicit "circle only, not asked for" decision on the just-shipped `Avatar` component. Asked via AskUserQuestion rather than guessing, since it's a revert-cost decision on an already-shipped, already-pushed component. User chose to keep `Avatar` circle-only — the screenshots' square shape is the legacy app's own quirk, not something to carry into the design system. New Message-family components use the existing circle `Avatar` as-is.
- Plan concluded: two new components (`Message` = rich no-bubble-chrome content block, `Bubble` = plain-text-only self/other reply) instead of one shared "chat message" component — same reasoning already established when Menu/Listbox got split (orient the boundary toward how the content genuinely differs, not toward what's convenient to build once). `EmbeddedEmailCard` and any `MessageScroller`-equivalent streaming/auto-scroll logic are explicitly deferred (one occurrence out of nine threads; streaming is real interaction logic outside a static-docs token system's current scope). `MessageThread`/`FilterPanel` are compositions of the atoms, not separate token files — same pattern Pagination's "full bar" and Listbox's "search + select-all" already used.
- Build order set: **Badge → Chip → Attachment → Message → Bubble → ThreadListItem → Composer**, each its own checkpoint (the plan explicitly does not greenlight building all 7 in one pass).

**Badge** (item 1) built immediately after plan approval — `tokens/components/badge.tokens.json` + `tools/build-badge-doc.mjs` → `docs/badge.html`. Non-interactive status/expiry pill: `Open`/`Resolved`/`Closed`/`Expires Jul 20`/`Expired Jul 12`. Zero new color tokens — every one of the 5 roles (neutral/primary/success/warning/danger) is a direct reuse of the existing `bg.*`/`text.*` semantic pair; `bg.*`'s own `$description`, written in the very first semantic-color session before any component existed, already reads "Passive status surfaces (banners, badges, notification chips)" — the third time this project has found a token's own description had already answered the question the component was about to ask. `neutral`'s pairing (`bg.neutral` + `text.default`) is the one exception to `bg.*`'s "never pair with text.default" line — justified explicitly in the token's own `$description` by the precedent Button secondary already set (`fill.neutral` + `text.default`, since no `text.neutral` role exists and gray has no colored text counterpart). Deliberately split from **Chip** (not yet built, the interactive filter-toggle sibling) — same Menu/Listbox-style ARIA-role reasoning: a status indicator and a checkbox-like control aren't the same thing wearing two colors. One size only, composed typography (not a `text-style.label-*` alias, same reasoning as Counter/Avatar's own labels — those bake in uppercase+wide tracking meant for words, wrong for sentence-case status text like "Resolved"). Wired into `nav.mjs`/`index.html`, all 28 docs pages regenerated clean, verified live (all 5 role pills + the thread-list-row usage sketch render correctly, resolved CSS custom properties match the 5 semantic pairs exactly).

## 2026-07-22 (cont. 10) — Badge generalized into a universal tag/label component

Right after Badge shipped as a narrow Message Center status pill, user asked to finish it properly in the same pass rather than leaving a half-scoped component other future use cases would just re-request: 3 sizes (sm 16 / base 20 / lg 24, kept `base` not the literally-requested `md` for the same cross-system consistency reason Avatar's sizing got renamed earlier this session), a `solid` opaque fill variant alongside the existing pale `tint`, and "all the other colors" — explicitly instructed to keep the new colors in a **separate role/category from the current ones**, not mixed in.

- Pulled real contrast data (`$extensions.hp.design/contrast.onWhite`) for every hue's 500 step before deciding anything, same rigor the original semantic-color session used for `fill.primary/success/danger/warning`: all 10 hues land 3.7–4.4:1 against white at the 500 step — confirms the already-accepted "large-text/UI-component AA, not 4.5:1 small-text, paired with semibold/bold weight" trade-off holds uniformly, not just for the original 4 roles. No hue needed a different step to stay safe.
- Modeled two independent axes, never combined: `role` (5 meaningful states, unchanged from the first pass) and `color` (10 decorative hues for generic tagging — gray/blue/red/green/amber/orange/violet/magenta/teal/brown — unrelated to status meaning, "color=orange" means "I want an orange label," not "this is a warning"). This is the literal shape of the user's "separate role/category, don't mix" instruction, and also mirrors the split already established for Avatar (identity-color fallback = decorative, unrelated to status) vs. the 4 semantic role colors.
- Rather than inventing 10 fresh hue×fill token pairs, checked for overlap first: 5 of the 10 requested colors (gray/blue/red/green/amber) are exactly the same hues the 5 existing `role`s already use. Made `color`'s entries for those 5 pure aliases of the role tokens instead of duplicate leaf colors — `color="blue"` and `role="primary"` now render from the identical CSS variable, confirmed by inspecting the generated `:root` block (`--tok-tag-blue-tint-bg` resolves to the exact same hex as `--tok-bg-primary`). Only orange/violet/magenta/teal/brown needed genuinely new leaf references. Added the new `tag.*` semantic group (`tokens/semantic/color.tokens.json`) to hold all 10 uniformly (aliases and new leaves alike), rather than special-casing the build script to pull 5 colors from one place and 5 from another — keeps the component's own `color` axis internally consistent regardless of which hues happen to be reused.
- `solid` fill reuses the exact `fill.*`+`text.onFill` pairing Button/Card/Menu already use for saturated fills (zero new tokens for the *pairing logic*, only for the 5 new hue values themselves). `neutral`/`gray`'s solid still pairs with `text.default`, not `text.onFill` — carried forward the Button-secondary precedent (`fill.neutral` is too pale for white text to read on) rather than blindly applying the white-text rule to every role.
- Sizing: resolved Counter's own real `sm`/`base`/`lg` values (height `dim.4/5/6`, paddingX `dim.1/1.5/1.5`, label `size.xs/sm/base`) and matched them exactly, rather than re-deriving new numbers from the user's literal 16/20/24px spec independently — same "resolve real values, don't retype" rule this project applies to color roles, extended to sizing. Kept `weight.semibold` (not Counter's `weight.bold`) since Badge shows sentence-case words, not digits — the one deliberate divergence, called out explicitly rather than blindly copying Counter's full recipe.
- Rewrote `tools/build-badge-doc.mjs` to resolve every `role`/`color` × `tint`/`solid` pair straight from `badge.tokens.json`'s own `$value` refs in a loop (never hand-typed a color per combination), and de-duplicated the generated `--tok-*` CSS variables so true aliases (e.g. `color=blue` and `role=primary`) share one variable instead of emitting two identically-valued ones under different names.
- **User's second question, answered directly, not just built around**: should Counter be rebuilt on top of Badge now that they share an identical size ladder? No — recommended and logged as resolved in `status.md`: Counter's whole reason for existing is contrast-matching an *already-colored parent surface* (`onPrimary`/`onNeutral`, tuned for sitting on a button fill), a structurally different concern from Badge's independent role/color system on a plain surface. The sizing match is a deliberate visual-rhythm choice (same reasoning Avatar and Button independently landed on the identical dim.8/10/12 grid without either being "built on" the other), not evidence they should merge. This also **resolves a long-open deferred item** — `status.md`'s "Open/deferred" list has carried "a standalone Counter variant for plain page surfaces" since the very first Counter session; Badge is now that component, so the line is marked resolved rather than left stale.
- Verified via computed styles across every generated class combination (30 role/color × fill pairs) — heights (16/20/24px), radius (999px), and every resolved bg/text color pair checked programmatically, confirming the 5 aliased colors are bit-for-bit identical to their role counterparts and the 5 new hues render distinct tint/solid pairs with white text on solid.
- Established as a **standing pattern for future small-pill components** (Chip is next): tint+solid as a paired fill axis, and role-vs-color as two separate, never-combined axes — logged explicitly in `status.md`'s locked-in conventions so Chip doesn't reinvent either shape from scratch.

## 2026-07-22 (cont. 11) — Badge: gray solid wasn't actually solid

User caught it immediately from a screenshot of the Colors section: `color="gray"` tint and solid rendered identically (same pale gray, same dark text) while every other hue's solid was visibly a strong, saturated fill. Root cause: `tag.gray.solid` had been aliased straight to `fill.neutral`, which is the exact same `color.gray.100` value `bg.neutral` (the tint) already uses — there's no actual "strong gray fill" among the existing neutral-adjacent tokens, `fill.neutral` is deliberately pale (it's Button secondary's own rest-state fill).

- Fixed by giving `tag.gray.solid` its own real value — `color.gray.500` + `text.onFill`, the same saturated-500-step + white-text pattern every other solid hue already follows (including gray's own missing case). Contrast checked before using it, not assumed: `color.gray.500` sits at 3.95:1 against white, squarely inside the 3.7–4.4:1 band already accepted for every other solid hue.
- Deliberately did **not** change `role="neutral"`'s own solid (still `fill.neutral`/`text.default`, unchanged) — that pairing matches Button secondary's established "pale even at emphasis" precedent, and a *status* of "neutral" has no real "urgent solid" need the way a decorative gray *tag* does. This makes gray the one hue in the `tag.*` group that aliases its role for `tint` only, not `solid` — documented explicitly in `tag.gray`'s own `$description` (added specifically to explain this one asymmetry) plus updated the group-level and `badge.tokens.json` descriptions, which previously and incorrectly claimed all 5 gray/blue/red/green/amber hues were full aliases.
- Verified via computed styles: `color-gray.solid` now resolves to `rgb(128,128,128)`/white (visibly distinct from tint's `rgb(232,232,232)`/dark text), while `role-neutral.solid` is untouched.

## 2026-07-22 (cont. 12) — Chip

Next in the Message/Bubble/Attachment build order. User asked for 3 sizes (sm 24, base 32, lg 40 — kept `base` not the literally-requested `md`, same cross-system reason as Avatar/Badge) and named concrete use cases: filter selection, a removable value chip for a Select's chosen result, icon-left, and icon+counter — plus asked to think through what else applies.

- Landed on two kinds sharing one shell, split the same way Menu/Listbox and Badge/Chip themselves already were: **toggle** (filter selection — checkbox-like, click flips checked/unchecked) and **removable** (a selected value with a trailing dismiss — existence *is* the selection, no checked ladder at all, just present-or-removed). Explicitly split from Badge too — Badge is the display-only sibling, Chip is the interactive one, same ARIA-role reasoning as every prior split in this project.
- Sizing: 24/32/40 turned out to be Button's own 32/40/48 ladder shifted down one `dim` step (`dim.6/8/10`) at every size — a chip is clickable like a button but reads as a small pill like Badge, so it makes sense it landed structurally between the two rather than needing new dimension primitives.
- **Real, not forced, interactivity** — the toggle chip is a genuine `<button aria-pressed="…">`, and the CSS keys its checked look off the `[aria-pressed="true"]` attribute selector directly, with a ~6-line click listener flipping the attribute. Every toggle chip on the docs page is actually clickable, not a static screenshot state — verified by scripting a real click and reading the resulting computed background color back (`false`→`true`, bg flipped from white to `fill.primary` blue in the same call). Matches the same "let the real cascade drive it" preference established for Checkbox's glyphs and Avatar's `onerror` swap.
- **Two checked treatments, not a state ladder** — solid (`fill.primary`+white, a real single boolean) vs. `checkedOutline` (`bg.primary` tint + border, literally Badge's own tint recipe plus a border). Reasoned through which content each fits: a chip carrying a `Counter` (e.g. a "Filters · 3" trigger summarizing several active filters inside a drawer) is an aggregate/trigger, not one boolean being on — filling it fully solid would overstate it, so it gets the quieter outline treatment instead. This is a genuine content-driven design call, not an arbitrary extra variant.
- Reused, not reinvented, three existing recipes: unchecked-hover border is `fill.primary`, the same "plain gray reads too weak a cue" rule Checkbox/Radio/Card's own unchecked-hover already established (checked here explicitly, not assumed); the counter surface (`onPrimary` on solid-checked, `onNeutral` on default/outline) follows the exact Button/Tabs surface-matching pattern, resolved from Counter's real token values; the removable chip's × button is Popover/Drawer/Modal's own close-button recipe verbatim (transparent → `fill.neutralHover` → `fill.neutralActive`).
- One deliberate asymmetry, called out rather than left implicit: removable-chip hover uses plain `border.strong`, **not** `fill.primary` — a removable chip isn't a toggle, hovering its body doesn't mean "clicking turns this on" the way it does for a filter chip, so it gets the ordinary Card-style hover instead of the toggle-specific activation cue.
- A computed-style check flagged `fill.neutralActive` and `text.disabled` resolving to the identical `rgb(180,180,180)` on the counter-in-chip story — investigated before assuming a bug, confirmed both are genuinely `color.gray.300` in the underlying palette (a real coincidence, not a wiring error) and moved on rather than "fixing" something that wasn't broken.
- Wired into `nav.mjs`/`index.html`; regenerated all docs pages clean; verified every size/state/content combination via computed styles (backgrounds, border colors, heights, radii) and the live click-to-toggle interaction, since the Browser preview tool's screenshot rendering was intermittently blank this session (confirmed a tool-side glitch, not a page problem, by cross-checking `get_page_text` and computed styles both came back fully correct).

## 2026-07-22 (cont. 13) — Sidebar nav couldn't scroll, Chip was unreachable

User flagged it with a screenshot: the sidebar nav list (now 23+ items) ran past the viewport with no way to scroll down to reach anything below Badge — Chip, just added, was invisible and unclickable. Root cause: `nav.side`'s CSS (`position: sticky; top: 0; height: 100vh`) never had `overflow-y: auto` — every one of this project's docs pages declares this same chrome block independently (no shared CSS module for the docs-site chrome, by design, confirmed going back to the very first pages), so the missing property was actually present in all 28 files (27 generated `tools/build-*.mjs` templates + hand-authored `docs/index.html`), not just the newest ones — it simply took the nav growing past ~15 items across enough sessions for anyone to notice.

- Fixed by adding `overflow-y: auto;` to every `nav.side` rule — 26 files shared the exact same single-line CSS string and were fixed with one `sed`, 2 (`docs/index.html`, `tools/build-colors-doc.mjs`) used an older multi-line format from earlier in the project and were fixed by hand. Verified zero files matching `height: 100vh` without `overflow-y: auto` remained afterward.
- Verified the actual fix, not just the diff: resized the browser viewport to 700px tall (well under the now ~1040px nav content height), confirmed via `getComputedStyle` that `overflow-y: auto` is live and `scrollHeight` (1040) genuinely exceeds `clientHeight` (700), then scrolled the nav to the Chip link and confirmed its bounding rect lands inside the viewport — not just that the CSS property is present, but that it actually resolves the reported problem.
- Regenerated all docs pages. Worth remembering for the next new component: this chrome block is meant to be copy-pasted verbatim (matches the project's whole pattern), and every current copy now includes the fix — no risk of reintroducing it by copying an old page, since there are no unfixed copies left.

## 2026-07-22 (cont. 14) — Attachment

Third piece of the Message/Bubble/Attachment build order. Modeled the anatomy on shadcn's own `Attachment` split (media/content/actions) researched during the original planning pass, but narrowed the state set from shadcn's full idle/uploading/processing/error/done to just the three the actual driving screenshots need: idle, uploading, done — no processing, no error, both flagged explicitly as deferred rather than silently absent.

- **Two different real HTML shapes, not just a CSS modifier, driven by a genuine accessibility constraint**: idle/uploading (composer, pre-send) is a plain `<div>` with one real nested `<button>` for remove/cancel. `done` (message display, post-send) is a real `<a download>` — and deliberately has *no* nested button, even though the natural first instinct is "same shell, just swap the icon and add a click handler." A `<button>` nested inside an `<a>` (or the reverse) is invalid HTML and a real accessibility footgun (nested interactive controls confuse both keyboard focus order and screen readers) — caught this before writing the markup rather than after, by asking what element the "done" download action should actually be (a link to a real file) rather than starting from "reuse the same button."
- Media: `icon` (a generic `insert_drive_file.svg` inside a raised `surface.default` square sitting on the card's own `surface.sunken`) finally cashes in a pattern flagged as worth reusing all the way back in the very first session (2026-07-20, Vercel's own post-deploy screen was noted then as a reference for "icon-in-a-soft-circle list rows") and never actually used until now — confirmed against `decision-log.md`'s own early entry before claiming the connection, not just from memory. `image` is a real `<img>`, `object-fit:cover`, filling the identical 40px box — 40px chosen specifically to match Avatar's own base size, so the two line up when both sit in a message row.
- `uploading`'s progress is plain text ("Uploading · 64%") in the same description slot idle's file-size text normally occupies, not a new progress-bar primitive — considered a real `<progress>` element or a custom bar, decided against introducing new visual surface for a state this system doesn't have a live upload pipeline to actually drive, matching the project's standing bias toward not inventing token surface without a concrete consumer.
- One size only (no sm/base/lg) and no `AttachmentGroup` (a horizontally-scrollable multi-file row) — the former because nothing calls for a size ladder the way Button/Badge/Chip's genuinely context-dependent density does, the latter because it's a composition of several Attachments plus a flex-wrap container, not new token surface, same reasoning already applied to Pagination's "full bar" and Listbox's search/select-all row.
- Verified via computed styles across every state/media combination — confirmed `idle`/`uploading` are real `DIV`s with a nested `BUTTON` (`hasNestedButton: true`, `isLink: false`) and `done` is a real `A` with none (`hasNestedButton: false`, `isLink: true`), the media box resolves to 40×40 with the correct background per variant (white square for icon, transparent for image), and the remove/cancel button's `aria-label` and the download link's `download`/`href` attributes are all present and correct — not just that the CSS renders, that the actual DOM shape matches the accessibility reasoning above.

## 2026-07-22 (cont. 15) — Message

Fourth piece of the family and the actual centerpiece of the original ask — the rich, institution-authored content block. User confirmed with a plain "так" to proceed per the plan's stated order.

- **Chrome default is none, confirmed against the live product over the mockup.** The plan itself already called this out as the recommended default; built it that way — a plain block (sender row + body, no bubble shape/border/fill), matching the real live desktop Message Center screenshots exactly. The staff-mobile mockups' white-bubble treatment for the same content is documented as an optional `chrome: card` variant (reusing Card's own exact recipe verbatim, not a lookalike) but is explicitly not the default — when a live product and an aspirational mockup disagree, the live product wins.
- **Reused two whole components instead of recomposing their look**: the sender avatar is Avatar itself, resolved from `avatar.tokens.json` (radius, base-size diameter, one identity hue) rather than redrawing a circle-with-initials from scratch; the CTA button is Button primary itself, resolved from `button.tokens.json` (radius, base-size height/padding, fill/hover colors) rather than a lookalike `.cta` class. Message's own token file only owns the layout rhythm (gaps) and body typography around them — same "resolve real values, don't retype" discipline this project has applied to every cross-component reuse since Tabs' counter.
- **One bold treatment, two jobs**: re-examined the actual screenshot copy ("Important Details" as a standalone bold line, "Dates:"/"Format:"/"Eligibility:" as bold run-in prefixes inside a sentence) and recognized both are visually the *same* idea — `weight.semibold` on `text-style.body-base`'s own size, no enlargement. Modeled it as exactly one token (`strongWeight`) applied either to a standalone `<p><strong>` or to a leading `<strong>` inside a paragraph, instead of inventing a separate "subheading" typography composite that would have duplicated the same visual weight under a different name.
- Confirmed, not assumed, that Message and the not-yet-built Bubble are split by content type rather than sender: re-checked the `Requirement Waiver Request` screenshot specifically, where the *staff member's own* short replies render as bubbles, not rich blocks — the same finding the original plan rested on, re-verified here since it's the exact boundary this component sits on one side of.
- **Caught the documented `resolveToken()`-drops-`$extensions` bug recurring, before it shipped this time** — `text-style.link-base`'s `textDecoration: "underline"` extension was, at first, silently unread again (`resolveToken()` only ever looks at a token's `.$value`), exactly the gap already fixed once on Listbox's "Clear all" link. This time it didn't visually break, purely by accident: the demo used a real `<a>` tag, and browsers underline anchors by default, so the *missing* CSS rule coincidentally produced the *correct-looking* result. Caught it anyway by checking the actual generated CSS for an explicit `text-decoration` declaration rather than trusting that the link looked right on screen, and fixed it the same way as before — fetch `$extensions` directly from the referenced node. Logged as a second occurrence in `status.md` specifically because "it happened to look right" is a worse trap than an obviously-broken link: it can hide indefinitely until a future change (a different element, a different browser default) exposes it.
- Also caught and fixed a small duplication in this build script's own `colorPaths` list (`border.default` listed twice, producing a harmless-but-sloppy duplicate `--tok-border-default` declaration) before shipping — checked the generated `:root` block directly rather than assuming the array was clean.
- Verified via `get_page_text` and computed styles (Avatar 40×40 with correct hue, sender name/meta weights and colors, `<strong>` at 600, link color + explicit underline, CTA button height/colors, `chrome: card` vs. chromeless backgrounds) and confirmed the full composed example's paragraph text matches the real Winter Intersession announcement content structurally. Wired into `nav.mjs`/`index.html`; all docs pages regenerated clean.

## 2026-07-22 (cont. 16) — Bubble

Fifth piece of the family. User again confirmed with a plain "так" to proceed per the plan's stated order. This was the one component the approved plan explicitly flagged as having a real open call to make while building, not before: `self`'s exact fill — the live desktop app shows a pale tint, the staff-mobile mockup shows a solid saturated blue.

- **Resolved the open call the same way Message's own chrome-default question was resolved**: made tint (`bg.primary`+`text.default`) the default, matching the live desktop app, and documented solid (`fill.primary`+`text.onFill`, the mockup's look) as the secondary option — not a coin flip, a direct application of the precedent Message already set this session ("when a live product and an aspirational mockup disagree, the live product wins"). Consistency between two decisions made hours apart in the same session, not two independent judgment calls landing the same way by luck.
- Deliberately used `text.default` (near-black), not `text.primary` (blue), for the tint bubble's text — considered pairing tint bg with role-colored text the way Badge's own tint does (`bg.primary`+`text.primary`), then rejected it: a whole paragraph of reply text in blue would read as a giant link, not a message. A bubble is a container, not a status indicator, so its text stays neutral regardless of the bg tint — a real distinction from how Badge/tag pills use color, worth keeping as a reference for any future tinted-surface decision in this family.
- **`radius.lg` (16px) instead of `radius.default` (8px)**, deliberately — reused the same "a bigger radius signals a genuinely distinct surface" reasoning Modal/Popover's own larger radii already established, rather than defaulting to the ordinary-control radius out of habit.
- Considered and rejected a chat-bubble "tail" (a less-rounded corner pointing toward the sender, the classic iMessage convention) — neither the live desktop screenshots nor the mobile mockups actually show one, both use plain uniform-rounded rectangles. Matched what the real references show rather than adding a detail from general chat-UI convention that wasn't actually asked for.
- **No sender-row tokens of its own** — reused the exact Avatar-based pattern Message's own sender row established this same session, mirrored left↔right (avatar-then-text for `other`, text-then-avatar for `self`) to match how the live screenshot actually lays out a self-reply's sender line (name+timestamp, then the small avatar, both pushed right). Verified the DOM child order programmatically for both roles rather than trusting the visual alone.
- Caught, before it shipped, the exact same `colorPaths` duplicate-entry sloppiness just fixed on Message minutes earlier (`text.muted` listed twice) — checked the array directly this time instead of waiting to spot it in the generated `:root` block, since the pattern was now a known thing to look for.
- Verified via computed styles: `self`/`tint` and `self`/`solid` resolve to the correct bg/text pairs, `other` resolves to Card's exact recipe, `radius.lg` computes to 16px everywhere, and a `border-color` reading on `self` bubbles that looked suspicious at first (`rgb(24,24,24)`) was confirmed to be the same harmless `border-style: none` non-issue already diagnosed once this session on Avatar's own live demo — checked `borderStyle`/`borderWidth` directly rather than re-flagging a false alarm.

## 2026-07-22 (cont. 17) — Message: added the Attachment slot that got missed

User caught a real inconsistency: Bubble (built right after Message) supports nesting a real Attachment, but Message — the component that's actually far more likely to carry a genuine file (a financial-aid notice with an attached form, a registrar letter with a document) — never got the same treatment in the first pass. A real oversight, not a deferred-on-purpose gap; fixed immediately rather than left for later.

- Added `body.attachments` to `message.tokens.json` — deliberately just a `gap` (`dim.1_5`, matching Attachment's own internal rhythm for when more than one is stacked) and a `$description`, no new color/radius tokens, since a body Attachment is the exact same real, unmodified Attachment component Bubble already nests — same "resolve real values, don't retype" reuse, just applied to the sibling that was missing it.
- `tools/build-message-doc.mjs` now resolves Attachment's own tokens (radius/padding/gap/media/title/description, same subset Bubble's build script already resolves) rather than copy-pasting Bubble's resolved CSS values — each build script independently resolves from the same source token file, so neither can drift from what Attachment itself actually defines.
- Added an "Attachment(s)" story to Anatomy (two stacked, showing the `dim.1_5` gap) and a new "With an attachment" full-usage section (a SAP-appeal notice with its submitted form attached) — not just a token addition, a demonstrated real use case, matching how every other body element (paragraph, list, image, CTA) already gets both an isolated anatomy story and a composed example.
- Updated the legend and top-level `$description` (both the token file's and the doc page's) to list Attachments among the supported body content types — they'd said "images, one CTA button" and silently skipped attachments even in the prose, not just the implementation, so both needed fixing together.
- Verified via computed styles: 3 real `.attachment` elements render across the two new stories (2 stacked + 1 in the full example), correct `surface.sunken` background, `radius.default` corners, correct titles/descriptions, and the stacked group's gap resolves to exactly 6px (`dim.1_5`) as tokenized.

## 2026-07-22 (cont. 18) — ThreadListItem

Sixth piece of the family, per the plan's own build order. Left-rail clickable row: subject + chevron, a Department/Received/Regarding/Expires-style 2-column metadata block, and a persistent "selected" accent.

- **Built on Card's `interactive` shell literally, not just "in spirit"**: loaded `card.tokens.json` in the build script and asserted, at build time, that ThreadListItem's hover/pressed/focused color refs are byte-identical to Card interactive's own resolved values — throws with a specific diff if they ever diverge, the same pattern already established for Button's primary/secondary/ghost size-grid identity check. This is stronger than the usual "resolve real values, don't retype" discipline: it's not just avoiding a hand-typed guess, it's a standing guarantee that catches drift automatically if either component's tokens change later.
- **`selected` reuses the exact pressed pair (`bg.primary`+`fill.primary` border), applied persistently** rather than defined as a new color combination — a selected thread is functionally "as if permanently pressed," so it gets the same visual language Card already uses for the ephemeral version of that state, not a third distinct treatment.
- **Reconciled a real disagreement between the two reference surfaces on how to show an expiring thread**: the live desktop app renders it as bare red-colored text ("Expires: 03/01/2027"), the staff-mobile mockup renders it as an actual Badge pill ("Expires Jul 20"). Neither "pick the live app, ignore the mockup" (the precedent from Message/Bubble) nor "pick the mockup, ignore the live app" was right here — resolved differently this time, on purpose: kept the live app's row/label structure (Department/Received/Regarding/Expires as plain label:value lines) but upgraded the specific Expires *value* to a real Badge (role=danger, tint, sm) instead of bare colored text, since Badge was explicitly built this session citing this exact row as its own driving use case — using it properly here closes that loop rather than leaving two different "this is overdue" visual languages (raw red text vs. a proper pill) coexisting in the same system.
- **Caught a real bug via computed styles, not just a passing build**: the Badge nested inside a story rendered fully transparent with black text — `bg.danger`/`text.danger` were referenced by the generated `.badge--role-danger` CSS rule but never added to this page's own `colorPaths` list, so `--tok-bg-danger`/`--tok-text-danger` were used but never declared in `:root`, silently falling back to CSS initial values. The printed CSS looked completely correct on inspection (the rule references the right variable names) — only checking actual computed `backgroundColor`/`color` in the browser exposed that those variables didn't exist. Fixed by adding the two missing paths; re-verified afterward that both Badge instances on the page resolved to the correct pale-red tint and danger text color, not just that the page rendered without a console error.
- Docs page demo is a real, working single-select list (click any of 3 rows, exactly one becomes `--selected`) rather than a static screenshot of one forced state — the same "let the real cascade drive it" bias as Avatar's `onerror` demo and Chip's `aria-pressed` toggle, extended to a proper radio-like list-selection behavior for the first time in this family.
- No unread indicator (dot or bold subject) — not asked for, explicitly deferred rather than added speculatively, called out in both the token file's own `$description` and the docs legend.

## 2026-07-22 (cont. 19) — Composer: the family's final piece, plan complete

Seventh and last component in the approved Message/Bubble/Attachment plan. User confirmed with a plain "так" again to proceed per the stated order.

- **Sourced 6 new Material icons before building anything else** — `format_bold`/`format_italic`/`format_underlined`/`local_offer`/`auto_awesome`/`send` didn't exist in the 136-icon set. Re-ran the exact method from the original 2026-07-20 icon-sourcing session rather than guessing paths: tried a direct `raw.githubusercontent.com` guess first (all 404 — wrong category assumptions), then fell back to the established, correct method — fetch the top-level repo contents via the GitHub Contents API to get `src`'s real SHA (confirmed again that the plain recursive-tree call truncates alphabetically before reaching `src`, matching the exact problem description from the original session), fetch that subtree recursively (27,133 entries, not truncated), and search it for the real paths. Learned along the way that Material's "Filled" style is just the bare `materialicons` folder, not a `materialiconsfilled` one as first guessed — fixed by inspecting an already-downloaded icon's real source structure via a known-working name (`add`) rather than continuing to guess. All 6 downloaded and post-processed identically to every existing icon (`fill="currentColor"` on the root `<svg>`). `docs/icons.html` picked them up automatically on rebuild (`142` icons) — that page reads the asset directory directly, no manual list to update.
- **Resolved the plan's one explicitly flagged open question**: formalized `bg.ai`/`fill.ai`/`fill.aiHover`/`text.ai`/`icon.ai` as real semantic roles rather than a one-off literal violet baked into Composer's own tokens. Applied the exact bar already set for `surface.inverse` (added specifically for Tooltip) — a genuine cross-product need, since both this composer and the not-yet-built AI chat product want one consistent "this is the AI feature" accent — and confirmed the need was immediate, not speculative: Composer needed this the same session, not hypothetically. violet was the natural pick, being the one primitive hue family with no existing semantic role.
- AI Assist's rest state deliberately reuses Badge's own tint recipe (pale `bg.ai` fill, no border) rather than a full solid button, and its hover deliberately reuses the "a border appears in the role color" language already established by Chip's unchecked-hover and Card's own hover — considered and rejected inventing a distinct hover shade for just this one button, since the pattern already existed and fit.
- **Field recipe is Input/Select/Search's own locked-in convention, verbatim** (`surface.sunken` + `border.default` → `border.strong` hover → `border.focus` focus) — not a new "composer field" look, and its value text is `size.md` (16px) for the identical Safari-iOS-auto-zoom reason already documented for Input's own value text. Checked the existing rule in `status.md` before inventing a font-size rather than picking one that happened to look fine.
- **Caught and fixed a real class-naming imprecision before it shipped**: icons sitting inside genuinely-real `<button class="btn ...">` elements (Send, Merge Tags) had initially been assigned `.composer__icon` instead of Button's own `.btn__icon` class, and the generated CSS sized them via `.btn--primary.btn--sm.btn--icon-only .composer__icon` selectors instead of the correct `.btn__icon` ones. Caught by checking what class name the *real* Button component (`button.html`) actually uses before treating "the icon renders and looks fine on this one page" as sufficient — fixed by renaming and adding proper `.btn__icon`/`.btn--ghost` CSS rules. Also caught, in the same pass, that the "Merge Tags" button's code sample had been printing `class="btn btn--ghost btn--sm"` while the page's own generated CSS never defined any `.btn--ghost` rule at all (it had been rendering correctly only via a leftover inline `style=` crutch) — this directly violates the project's core "the generated `<style>` block IS the printed code" rule established since Button's very first build. Fixed by adding real `.btn--ghost`/`.btn--ghost.btn--sm` rules (resolved from `button.tokens.json`, not retyped) and removing the inline style entirely.
- Also caught, separately, that Merge Tags had been left out of the actual rich-composer toolbar markup despite being named in the plan's own anatomy and present as an isolated story — added it to both the live markup and its hand-prettified code sample.
- Verified via computed styles: simple Send is a real 32×32 icon-only primary button (`fill.primary`); rich Send is base-size with visible "Send" text; AI Assist resolves to the exact violet tint/text designed (`#e8e4ff`/`#743ed8`); Merge Tags appears twice (isolated + in-context) with correct ghost styling; the Switch defaults checked with a blue track; the field resolves `surface.sunken`/`border.default`; and real typing into both the simple `<input>` and the rich `<textarea>` confirmed both render at 16px, not just that the token said so.
- This closes the approved plan (`/Users/mac/.claude/plans/distributed-sprouting-gadget.md`) — all 7 pieces (Badge, Chip, Attachment, Message, Bubble, ThreadListItem, Composer) built in the stated order, each its own checkpoint, none skipped or reordered.

## 2026-07-24 — DS/Designs sidebar split + the first Designs entry: Student Message Center prototype

The docs site now has two faces, switched by tabs at the top of the sidebar: **DS** (the design system — everything that existed before) and **Designs** (a prototype explorer: an accordion per product, holding interactive prototype pages under `docs/designs/`). Built per explicit request, together with the first prototype.

- **Nav stayed a one-file change on purpose**: `tools/lib/nav.mjs` emits its own `<style>` + `<script>` inline inside the nav block, so the 33 per-page chrome CSS templates didn't need touching — rebuild-all picked the tabs up everywhere. The pane containing the active page is the one shown on load; the tabs only toggle in-page, no navigation, no localStorage. `renderNav` gained a `basePath` option because designs pages live one directory deeper. `docs/index.html` (the one page with no generator) was hand-updated with the same block — same trap as the Sora `@font-face` link, worth remembering it exists.
- **Designs pane structure**: `DESIGN_PRODUCTS` in nav.mjs — product accordion (`<details open>`, "Message Center") → prototype links. Grows by adding entries there.
- **Two generated pages per prototype, one build script** (`tools/build-design-student-message-center.mjs`): a *viewer* (docs chrome + a real DS segmented Tabs for Mobile/Tablet/Desktop viewport switching + an iframe, mobile-first default) and the *app itself* (standalone, no docs chrome, `docs/designs/student-message-center-app.html`, also linked as "Open standalone ↗").
- **The prototype is built strictly from DS components, every recipe resolved from its own token file** — tabs/search/select/listbox/thread-list-item/badge/avatar/message/attachment/bubble/composer/button/separator all load their real token JSONs through the same registry/resolve helpers every build script uses. The only non-component CSS is the `mc-*` composition layer (app shell, panes, breakpoints) — the app-layout equivalent of a docs page's chrome. Breakpoints are literal 768/1024px, per Grid's own "structural, not tokenized" precedent.
- **Functional scope is the live student desktop app; the staff mobile mockups were visual direction only** — no flags, no "Handled by", no filter chips, no Resolve. Thread rows use the **inbox shape** of ThreadListItem per explicit request, even though the live student app renders the older card/metadata shape — the newer shape is the direction the user wants.
- **Sort By is the Select-trigger + Listbox composition** status.md had already flagged as Select's natural next step — first live wiring of it: a `.select` (base, floated label) as a `popovertarget` trigger, a single-select `.listbox` panel with the always-rendered-hidden-checkmark treatment (the "render state glyphs, hide via CSS" convention), native popover positioning script same as Popover/Menu.
- **All interactions are real, none staged**: single-select thread rows across both lists; Inbox/Archived tab switching; live search filtering with a live "N THREADS" count and a clear (×) that only shows populated; sort genuinely reorders rows (Date=authored order via data-idx, Subject/Department localeCompare); Send appends a real self Bubble (tint — the default per Bubble's own live-app call) with the typed text injected via textContent, never innerHTML; Archive really moves the row into the Archived list and returns to the empty state; Print calls window.print(). Mobile is a one-pane flow — rail → thread with a ghost icon-only back button, hidden ≥768px.
- **Verified via computed styles and scripted interaction, not just a passing build**: badge warning/danger tints resolve to real colors (the transparent-Badge trap re-checked), appended bubble bg = bg.primary, selected row = bg.primary + fill.primary border, composer input = 16px (Safari-zoom rule), mobile one-pane open/back both directions, archive moves 2→3 rows and counts update.
- **Missing icons, noted not blocking**: `archive`, `inbox`, `smartphone`, `tablet` aren't in the 142-icon set. Archive is a text button (matches the live app), device tabs are text-only. Source them via the established GitHub-API method if icon versions are ever wanted.
- **Tool artifact worth remembering**: in the embedded preview browser, the viewer's iframe width *transition* appears stuck (background tabs don't tick animations) — the 768px tablet width verifiably applies the moment the transition is disabled, and real browsers animate it fine. Same family of non-bugs as the blank-screenshot-at-scroll glitch.
- **Post-deploy 404 fix**: `vercel.json` rewrites only covered `/` and single-segment `/:page.html` → `/docs/:page.html`, so the two-segment `/designs/:page.html` URLs 404'd on the live site (worked locally because the local server serves the real `/docs/...` tree directly). Added a third rewrite for `/designs/:page.html` → `/docs/designs/:page.html`. Any future nested docs directory needs its own rewrite line — Vercel's `:page` param doesn't span `/`.

## 2026-07-24 (cont.) — Sender-row rebalance, Message card chrome in-thread, ThreadListItem inbox full-bleed + unread/read

Three explicit feedback points on the first prototype round, all pushed down into the components themselves (per the user's own framing), then re-consumed by the prototype:

- **Message gets the white bubble in the thread view**: an institution message rendered chromeless directly on the page read oddly in the Message Center thread pane — the existing `chrome: card` variant (Card's own recipe, zero new tokens) is now what that surface uses. This partially reverses the original "live product outranks mockup" call for this one surface, on explicit request; the component's own default stays chromeless, and `message.chrome.card`'s `$description` records both facts.
- **Sender-row rebalance (Message + Bubble + prototype, one decision)**: avatar drops from base (40px) to **sm (32px)** — 40px next to a single small text line read top-heavy — and the sender *name* moves from `text.default` to **`text.secondary`** so it stops competing with the body (timestamp stays `text.muted`, still one step quieter). Implemented as a real token change (`message.sender.nameColor`), and Bubble's docs now resolve that exact token from message.tokens.json for its own sender name rather than retyping a color — the build scripts had also both been hardcoding `cv("text.default")` for the name instead of reading the `nameColor` token that already existed; fixed in passing. The user offered "smaller avatar OR two-line name/time" — picked the smaller avatar, keeping the one-line sender the component was built with.
- **ThreadListItem inbox shape redesigned to the staff-mockup list language**: no longer a rounded bordered card in a padded/gapped list — **full-bleed flat rows** (no radius, no own border, no gaps) separated by a 1px `inbox.list.divider`, filling the rail edge to edge. And the deferred-from-day-one unread indicator finally exists, as the mockup's two-state pair rather than a dot: every row is **`unread`** (surface.default bg, semibold text.default identity/subject, text.secondary preview) or **`read`** (surface.sunken bg, normal-weight text.secondary identity/subject, text.muted preview). `read.bg` is `surface.sunken`, *not* `surface.page` — first attempt used surface.page before checking the resolved value, which is literally white (same as unread's surface.default), so it dimmed nothing; surface.sunken's own `$description` explicitly sanctions "table row stripes", which is exactly what a read row is. Hover/pressed move off the card shell's blue-border language (a flat row has no border to color) onto the established ghost `fill.neutralHover`/`fill.neutralActive` progression; selected keeps persistent `bg.primary`, borderless; the focus ring flips inward (negative outline-offset) since full-bleed rows have no outside gap for it. The card metadata-row shape keeps its original Card-interactive shell untouched.
- **Opening a thread now marks it read** — in both the docs in-context demo and the prototype, click = select + unread→read swap, the real product behavior, so the state transition is live rather than two forced screenshots.
- **Caught the documented colorPaths trap again before shipping**: the docs page's new `read.bg` rule referenced `--tok-surface-sunken` without declaring it in that page's `colorPaths` — the exact transparent-Badge failure mode from the ThreadListItem session. Added, and this time also ran a mechanical check over all four rebuilt pages (every `var(--tok-*)` usage grepped against its `:root` declarations) instead of trusting spot-checks.

## 2026-07-24 (cont. 2) — Rail rework round 2: surface.dim, flag toggle, segmented tabs + counter, filter chips, cross-tab search

Second feedback round on the prototype rail, again pushed into components/tokens first:

- **`surface.dim` added to the semantic layer** (`{color.gray.50}`) — read rows on surface.sunken (gray.100) were called out as too heavy next to unread's white ("десь 25 чи 50"). gray.50 picked over gray.25 (25 is nearly invisible on white). Scoped like sunken: de-emphasized rows only, not a generic nested-container bg. ThreadListItem `inbox.read.bg` now points at it; the semantic-colors doc picked the new role up automatically.
- **The inbox flag became a real per-thread importance toggle** (student marks things for themselves): outlined flag (`icon.muted`) off, filled flag (`icon.warning`) on, both glyphs always in the DOM and swapped via the button's `aria-pressed` (the Checkbox render-all-glyphs rule). Sourced the repo's **first outlined Material icon** (`assets/icons/material-outlined/flag.svg` — new directory; `docs/icons.html` only reads material-filled, so it won't list there). Because the row now nests a real button, **the inbox row itself became a `<div role="button" tabindex="0">`** with hand-wired Enter/Space — a real `<button>` can't nest another, the exact resolution Attachment's idle shape already used. Flag clicks stopPropagation so they don't open the thread.
- **Search moved above the tabs and became cross-tab**: while a query is present the Inbox/Archived split is suspended — both lists render as one combined result set and every row reveals a scope Badge (neutral tint, "Inbox"/"Archived") on the same line as its Expires badge. The badge is always in the markup, shown via CSS only under `.mc--searching`; archiving a thread rewrites its scope badge text. Two cascade bugs caught by computed styles here, not by reading the CSS: the flag-glyph swap lost to `.flag-btn svg`'s higher specificity (both flags rendered), and `.badge`'s own `display:inline-flex` outranked the scope badge's `display:none` because the badge block came later in the sheet — fixed by specificity/order, and both are the same genre as the documented colorPaths trap: the generated CSS *looked* right.
- **Inbox/Archived switched underline → segmented** ("таби в блоці"), with a real **Counter** (base, onNeutral — resolved from counter.tokens.json exactly like Tabs' own docs page) showing the live unread count on the Inbox tab: decrements as threads are opened, hides at zero, swaps active/inactive surface with the tab.
- **Sort By is gone, replaced by a filter-chip row**: Unread / Expires soon / Flagged — real Chips (base, toggle, `aria-pressed`), freely combinable AND-filters. "Expires soon" matches any row carrying an Expires badge, expired included, per the explicit ask.
- **Department filter = a "dropdown chip"** — the user's own open question ("це який компонент тоді?"). Answer settled: not a new component, a composition — **Chip as the trigger + the same single-select Listbox popover** Sort By used (Select+Listbox in Chip's clothes; Material's "filter chip with menu" is the same idea). Single-select on purpose: "All departments" or exactly one. While a department is active the chip shows the department name in Chip's own **checkedOutline** treatment — the aggregate/trigger semantics that state was literally designed for ("Filters · 3").
- **The THREADS divider now runs edge to edge** — the count label keeps its own padding, the separator escapes it (explicit ask: "лінію треба доводити аж до самих бордерів").
- Verified by scripted interaction + computed styles: one glyph per flag state (pell's filled flag = #b26f07/icon.warning), scope badges hidden→shown around search, combined search finds archived "Study Session" from the Inbox tab, each chip filters correctly, department pick relabels+outlines the chip and filters both lists, unread counter 2→1 on read, separator width == rail width.

## 2026-07-24 (cont. 3) — Two real bugs the user caught + search-as-a-button

The user reported "filters don't work" and "search splits the screen in half" — both real, both shipped in the previous round, and both invisible to the checks that round ran:

- **`[hidden]` rows never actually disappeared.** The UA stylesheet's `[hidden] { display: none }` loses to ANY author rule — `.thread-item-inbox { display: flex }` overrode it (author beats user-agent regardless of specificity). Filtering logic was fine; every verification read the `.hidden` DOM property instead of computed `display`, so the tests passed while rows stayed visible on screen. Fixed with an explicit `.thread-item-inbox[hidden] { display: none; }` in both the docs page and the prototype, and the lesson appended to the "check computed styles" discipline: *a passing `.hidden` property check is not a passing `display` check*.
- **Search split the rail into two half-height scroll panes** — with both lists un-hidden during a search, each `.mc-list` was its own `flex: 1; overflow-y: auto` box. Fixed structurally: both lists now live inside one `.mc-rail__lists` scroll container (lists themselves no longer grow or scroll), so search results render as one continuous combined list — which also matches what the combined-search design intended in the first place.
- **Search is now an icon button beside the tabs** (a real Button ghost base icon-only), per explicit request: clicking it swaps the segmented tabs for the expanded field (`.mc--search-open`), autofocuses, and the × clears a non-empty query / closes an empty one (Escape closes too). The always-on search field above the tabs is gone.
- **Flag enlarged**: `inbox.flag.iconSize` dim.4→dim.5 (16→20px), hit target 28px (padding dim.1, negative margin so row height doesn't inflate). Also answered what it *is*: ThreadListItem's own flag slot (mini ghost-hover toggle defined by the component's tokens), deliberately not the Button component — Button ghost's smallest icon-only is 32px, too large inside a row.

## 2026-07-24 (cont. 4) — EmptyState component + rail round 3 (dead tabs, 40px tabs, search button bg, three expiry tiers)

- **EmptyState is a new component (#29)** — `tokens/components/empty-state.tokens.json` + `docs/empty-state.html`, text-only variant per explicit spec: a box that fills the area its content would occupy and centers one body-size hint on a long, weak, fully-rounded gray pill (bg.neutral — Badge's own neutral-tint fill — + radius.full, paddingX wider than a Badge's so it reads "long"). Purely presentational, Separator/Box category. Heading/illustration/action variants deferred until a real surface needs them. Wired into nav (rebuild-all) and index.html's hand-maintained card grid.
- **Applied in the prototype on all three empty surfaces**: "Choose a Thread" (reading pane, replacing the old bare title-2xl text — that one-off styling is gone), the type-to-search state (search open, no query yet: lists AND the count row fully replaced by a "Search across Inbox and Archived" hint), and zero results (search or chip filters — "No threads found" / "No threads match the filters").
- **"Archived became unclickable" — a real regression from the topbar rename**: renaming `mc-rail__tabs` → `mc-rail__topbar` updated the listener-attach selector but missed the *second* occurrence inside the handler (the class-toggle loop), so lists switched while the active-tab classes silently didn't — and my first fix attempt only caught the first occurrence too. Greped for the stale selector after the fix this time. Lesson recorded: when renaming a selector, grep the build script for every occurrence, not the one the error message points at.
- **Tabs 40px total**: segmented pair switched from base (40px item + track padding = 48) to **sm (32px item + 4px track padding = 40)**, unread Counter follows down to sm — same tab-size/counter-size pairing Tabs' own docs page uses.
- **Search trigger got a background**: Button ghost → **Button secondary** (fill.neutral) icon-only base, per explicit ask.
- **Three expiry tiers in the data now**: soon (warning tint — FAFSA Verification), expired (danger tint — Advising Appointment), and far/not-soon (**neutral gray tint — Transcript Ready, and Pell reclassified**: its 03/01/2027 date was never "soon"; it had been warning only because the tier didn't exist). The "Expires soon" chip matches warning+danger only — a far-off neutral expiry deliberately doesn't count, per explicit spec. Inbox is 9 threads now, unread 3.
- The search-open button now calls applyFilter() immediately, so the type-to-search EmptyState appears on open, not on first keystroke.

## 2026-07-24 (cont. 5) — Context-aware filter chips

- **Archived shows only Flagged + Department** — Unread and Expires soon are Inbox-only concepts (an archived thread is by definition dealt with), hidden via an `mc--archived` class per explicit request. Switching to Archived also *resets* the two hidden chips (aria-pressed + state), so a chip that's no longer visible can't keep filtering invisibly — the same trap class as the earlier "hidden rows still displayed" bug, caught at design time instead of by the user this round.
- **Search shows no chips at all** — search is global, the whole filter row leaves while search is open (`.mc--search-open .mc-rail__chips { display:none }`), and opening search resets every chip filter including Department for the same no-invisible-filtering reason. Closing search brings the row back in its clean state.

## 2026-07-24 (cont. 6) — Row avatars sm, search-mode spacing + a dedicated Close

- **ThreadListItem inbox avatars base→sm (40→32px)** per explicit request — same size the sender-row rebalance already moved to, so every avatar in the Message Center rail and thread view is now the sm step. Token $description and both build scripts updated (docs page resolves avatar.size.sm now).
- **Search mode breathing room**: with the chips row gone while search is open, the count label sat flush under the field — the topbar now carries dim.3 (12px) bottom padding in search mode only.
- **Search close is its own control now**: a blue "Close" label (text-style.link-base + text.primary, $extensions decoration fetched directly — the documented resolveToken gap) beside the field, per explicit UX feedback that the dual-purpose × was confusing. The in-field × returns to Search's own canonical convention: visible only once populated, and it only clears. Escape still closes.

## 2026-07-24 (cont. 7) — Thread header rework + Message card chrome wraps the body only

- **Message's card chrome corrected to wrap the BODY only** — the first cut wrapped the whole component, so the sender row (avatar/name/time) floated *inside* the white card, which the user rightly flagged: everywhere else the sender row sits above the bubble. Same tokens, different application (`.message--card .message__body { … }`); token $description + docs page updated. This also crystallized the answer to the user's "чи взагалі потрібен окремий Message — це ж той самий Bubble з напиханим усім": the shells are now visibly the same anatomy (sender on top, chromed surface below), and they stay separate components because their CONTENT contracts differ — Bubble is deliberately capped at text ± one Attachment so replies can't grow into mini-sites, Message is the contract for rich institution content (headings/links/lists/images/attachments/CTA). Merging them would mean one component with two incompatible content models — the exact thing the Menu/Listbox and Badge/Chip splits exist to avoid.
- **Thread header rebuilt mobile-first per explicit feedback**: on mobile the top row is [← Back (a real Button ghost sm, icon+label now, not icon-only)] … [Archive · Print], the subject drops BELOW them (heading-md, 16px — was title-xl and got crushed between the buttons), then the Department/Institution tag pills; the bar's own bottom border is the separator under the tags. On ≥768px the back button hides and the subject shares the row with the actions (heading-lg), reflowed purely via flex order — one markup.
- **Status metadata dropped; Department/Institution became tag pills** — the EmptyState pill language (bg.neutral + radius.full) at a smaller size (body-sm/12px, tighter padding), "Department: …" prefix in text.muted, value in text.secondary, inline one-after-another with no separators of their own. A prototype-level composition (`mc-thread__tag`), not a new component — it's Badge-adjacent, but a labeled key:value pill is a different thing from Badge's single-word status; worth formalizing only if a second surface needs it.
- Verified mobile (375) and desktop: header stacking vs. single-row subject, tags wrap gracefully when narrow (the embedded preview's "desktop" is 800px, so its 480px reading pane wraps them — a real ≥1280 desktop fits them inline), sender row above the white bubble in both.

## 2026-07-24 (cont. 8) — Tablet frame off-by-2px + header polish round

- **"Tablet looks broken" was a real off-by-2px bug in the viewer, not the app**: the device frames use border-box, so `.device--tablet { width: 768px }` left the iframe's INTERNAL viewport at 766px — the app's own `min-width: 768px` split-view media query never fired and the tablet frame rendered the one-pane mobile layout. Frames now add +2px for their own borders (377/770), making the inner viewport exactly 375/768. Worth remembering for any future device-frame viewer: the media query runs against the iframe's content width, not the frame's outer width.
- **Subject-inline moved from ≥768 to ≥1024**: the 768–1023 split view's reading pane (~450px) is too narrow for subject + actions on one row, so tablet keeps the stacked header (actions top-right, subject below, tags under) and only ≥1024 goes inline.
- Per same-session feedback: Archive/Print dropped to **sm** secondary buttons; the subject is **heading-lg (18px)** at every width now; on mobile the Department/Institution pills show **values only** ("Academic Advising") — the prefixes ("Department:") return at ≥768 where there's room; tag pills are a single non-wrapping row (overflow-x scroll) so the header never grows a fourth line.

## 2026-07-24 (cont. 9) — Tablet header goes inline too

- The tablet-only stacked header (actions-only top row, subject below) got called out immediately once the split view actually rendered — an actions-only first row reads as a hole. Reverted the cont. 8 exception: **subject shares the row with the actions on every split view (≥768), wrapping to a second line when it doesn't fit**, instead of dropping below the buttons. The stacked header is now mobile-only (<768), where the Back button justifies the separate top row.

## 2026-07-24 (cont. 10) — Value-only header pills everywhere + Expires/Archived badges in the thread header

- The "Department:"/"Institution:" prefixes are gone at EVERY width now (the ≥768 return of the prefixes lasted one round) — "Financial Aid · PeopleSoft University" reads fine bare, per explicit call.
- **The thread header's tag row now also carries the thread's state badges**: a real Badge (BASE size — the row list keeps sm) for Expires (warning/danger/neutral, same role as the row's own badge) and an "Archived" neutral Badge on archived threads. Badge CSS refactored into `.badge--sm` / `.badge--base` size modifiers to support the two sizes side by side. Archiving a thread live also appends the Archived badge to its open header, so the pane can't disagree with the list it just moved to.

## 2026-07-24 (cont. 11) — Header tag row wraps instead of scrolling

- The nowrap+overflow-x header tag row clipped badges mid-word ("Expi…") on mobile/tablet — switched to flex-wrap at every width, per explicit request: the row grows to a second (or third) line when tight, a horizontal scroll never hides a state badge. The one-line-scroll idea lasted exactly one round against real content widths.

## 2026-07-24 (cont. 12) — Header meta: pills → one plain 12px line

- Department/Institution stopped being pills after all: now one plain body-sm (12px) text.secondary line — "Financial Aid · PeopleSoft University" (middle dot separator) — at every width, per explicit call: pills carried too much visual weight for what is quiet context. The Expires/Archived state Badges keep their tinted pill form right after the line — state stays loud, context goes quiet, which is the correct hierarchy the pill version was muddling.

## 2026-07-24 (cont. 13) — Header state badges down to sm

- The header's Expires/Archived badges dropped from base to **sm** (the same size the thread list rows use), letting them sit inline right after the "dept · institution" line even on mobile — base-size badges pushed onto their own line at 375px. The short-lived `.badge--base` modifier is gone from the prototype CSS again; wrap remains as the fallback for genuinely long combinations.

## 2026-07-24 (cont. 14) — Toast component (#30) + blue Inbox scope badge

- **Terminology research settled the naming question the user asked ("toast, sonner… не розумію різницю")**: transient, self-dismissing feedback is a *toast* everywhere — Radix Toast, shadcn's toast, Material's Snackbar (bottom variant), Ant's `message` (top-center variant, the closest match to this request); **Sonner is just a React toast library**, not a distinct pattern; "notification" conventionally means the richer, longer-lived title+body+actions card. Component named **Toast** accordingly; the rich notification variant is explicitly deferred.
- **Toast (#30)**: one line — role icon + text — fixed top-center (`toast.offsetTop`), auto-dismissing (3200ms default in the helper). Three roles per the request (success/danger/warning); the surface stays neutral at every role (Popover's own floating recipe: surface.default + border.default + shadow.sm + radius.default) and ONLY the icon carries meaning — **first consumer of the `status.*` semantic group**, whose own $description reserved it for "badges, alerts, form validation" since the semantic layer was built. Icons: check_circle/error/warning from the shared set. Native-first like every overlay: **`popover="manual"` + showPopover()** — free top-layer, and `manual` opts out of light-dismiss (a toast leaves on its own timer, not on outside clicks); entry via @starting-style, same as Drawer/Modal. Deferred: close ×, action link, stacking manager.
- **Prototype**: archiving now confirms with a success toast ('"<subject>" archived') — the driving use case the component was built for.
- **Search scope badge "Inbox" is blue now** (Badge role=primary tint — bg.primary + text.primary) per explicit request; "Archived" stays neutral gray, and the archive handler swaps the moved row's badge role along with its text so an archived row can't keep a blue Inbox pill.

## 2026-07-24 (cont. 15) — Mobile thread drops the app topbar; body-link rule leaked onto Attachment

- **On mobile an open thread now takes the whole screen**: the app's own "Message Center" topbar hides under `.mc--thread-open` (<768 only — split views keep it), the thread bar with Back becomes the top of the screen. One less stacked header on a small viewport.
- **The blue underline on Attachments was the `.message__body a` link rule leaking**: Attachment's done shape IS an `<a download>`, so the body's link styling (text.primary + underline) hit it and drew through the title/description. Fixed with a scoped `.message__body a.attachment { text-decoration: none; }` override. Worth remembering for real ports: any generic `a` styling inside Message's body needs to exempt component anchors nested in the flow (Attachment today, anything link-shaped tomorrow) — the docs pages never caught this because their body-attachment stories use `<span class="attachment">`, not the real done-shape anchor.

## 2026-07-24 (cont. 16) — Sidebar branding: Highpoint logo + per-pane titles

- The sidebar now leads with the **Highpoint logo** (new source asset `assets/highpoint-logo.svg`), inlined by `renderNav` at build time rather than <img>-linked — no basePath juggling for the designs/ pages, and the wordmark's #090D19 text fill is swapped to `currentColor` so it follows the docs chrome's text color in dark mode (the blue mark + white glyph stay fixed brand colors).
- The "hp-design / Highpoint design system" brand block is gone; each pane now titles itself UNDER the tabs — **"Design System"** on the DS pane, **"Design Prototypes"** on the Designs pane. One-file change in nav.mjs + the usual hand-sync of docs/index.html (its no-generator sidebar caught up in the same pass).

## 2026-07-24 (cont. 17) — Card shape removed from ThreadListItem + strictness audit

- **ThreadListItem is single-shape now**: the original card/metadata-row shape (subject + chevron + Department/Received rows, built on Card's interactive shell with the build-time assertSameRef check against Card's real tokens) was removed per explicit request — the inbox redesign fully replaced it in the prototype and keeping a dead shape documented as current would mislead a real port. Tokens trimmed (radius/gap/bg/border/divider/chevron/metadata/state.hover/state.pressed gone; state.selected keeps only bg — the "persistent pressed" meaning survives the shell it came from), docs page rewritten around the one shape, and with the card shape went the component's Card lineage: the assertion is gone because there is nothing left that mirrors Card.
- **Strictness audit (prototype ↔ components ↔ tokens), mechanical where possible**: (1) zero hex colors outside `:root` in the app page; (2) every font-size in the app resolves from the token size scale (10/12/14/16/18/20 = xs…xl, verified by grep); (3) every `var(--tok-*)` declared on all rebuilt pages. Two provenance violations found and fixed: **Bubble's sender row carried a literal `font-size: 12px` / `font-weight: 600`** in both the Bubble docs page and the prototype — now resolved from `message.sender.name`/`meta` typography tokens (which it always claimed to reuse), same for the sender gap; and the prototype's selected-row background was written as `bg.primary` directly instead of resolving `threadListItem.state.selected.bg` (same value, wrong provenance — now resolved). Deliberate literals that remain, each with a precedent: 768/1024 breakpoints (Grid's "structural, not tokenized"), max-widths 640/320 (layout decisions, literal in the component docs too), attachment content 2px gap (literal in Attachment's own docs), viewer device frames (docs chrome).
- **Cold memory written** per explicit request — two files in the project memory dir: a 7-point generated-page verification checklist (colorPaths grep, computed-styles-not-properties, `[hidden]` vs author rules, concatenated-sheet specificity, device-frame border-box +2px, embedded-preview quirks, serve-from-repo-root) distilled from the five bugs that passed builds this week, and the user's iteration-style profile (screenshot rounds, push-every-round, loud-state/quiet-context taste, component-level fixes first).

## 2026-07-26 — Client feedback round: mixed sender + reply indicator (ThreadListItem)

External client feedback on the prototype, two of three items landed immediately (the third — mobile chips scroll — is under option review):

- **Mixed sender** (`inbox.identitySecondary` tokens): a thread's sender is now a department (announcements) OR a person — "Ava Robinson · English Dept". The person leads in identity's own semibold; the " · Department" suffix renders in identitySecondary (normal weight, text.muted), so the person carries the line and the department quietly contextualizes. The Avatar keys off whichever name leads. Identity line ellipsizes (flex:1 min-width:0) — long combos crop rather than push the date. Rationale from the client: students build relationships with individual advisors, department-only senders read impersonal.
- **Reply indicator** (`inbox.reply` tokens + material reply.svg, sourced same day): an affirmative, non-interactive muted reply icon on the PREVIEW line (user's explicit placement call: the message line, not the expires row — the preview just crops a little earlier) marks threads that accept replies; absence = announcement/closed. Inside the thread view the same fact shows as the Composer's slot holding EmptyState's quiet pill ("This thread doesn't accept replies") instead of the composer — component reuse rather than a bespoke hint style. Prototype data now carries sender/replies per thread; 5 of 10 threads replyable.

## 2026-07-26 (cont.) — Mobile chips: the "Filters · N" dropdown-chip (option B)

- The client's third item (awkward horizontal chip scroll on mobile) went through an option review with the user — four variants were mocked up (A wrap-to-two-rows, B Filters-chip with counter, C bottom-sheet Drawer, D hide-scrollbar cosmetics) and **B won**: on <768px the three toggle chips collapse into one **"Filters · N" dropdown-chip** — filter_list icon + label + a real Counter (sm, onNeutral) that appears when any filter is active, with the chip flipping to **checkedOutline** (the exact aggregate-trigger case that Chip state was designed for). It opens the **multi-select Listbox with real Checkbox markers** (the listbox.checkbox recipe, first live use in the prototype). Department stays its own chip; ≥768 keeps the inline toggle chips and hides the Filters chip.
- Both surfaces (inline chips ↔ listbox checkboxes) drive the same filter state and stay in sync in both directions, so resizing across the breakpoint never desyncs; resetChipFilters (Archived switch, search open) clears the checkboxes and the counter too. Archived hides the Unread/Expires OPTIONS inside the listbox the same way it hides those chips — via `.mc.mc--archived` double-class selectors, deliberately over-specific so the ≥768 visibility reversal (same specificity, later in the sheet) can't override them: the cascade-order trap from cont. 5, dodged at design time this round.
- Verified by computed styles (the screenshot tool served stale frames again — the known glitch; JS state checks were consistent): mobile shows Filters+Department only, checking Unread filters rows/updates counter/outlines the chip/syncs the desktop chip, Archived hides+resets the two inbox-only options.

## 2026-07-26 (cont. 2) — Reply indicator v2: gray icon → green "Replied" Badge

- The muted reply icon lasted one round ("сіра і не понятна") — replaced by a **real Badge: sm (16px), role=success, tint** (pale green bg + active green text), label "Replied", on the same preview-line spot. First use of Badge's success tint in the prototype. `inbox.reply` keeps no tokens of its own anymore — it IS a Badge resolved from badge.tokens.json, same pattern as the Expires badge; reply.svg stays in the icon set but unused here.
- Note for a future round: the client's original ask was "know whether a message is REPLYABLE before opening"; the label currently reads "Replied" per the user's explicit wording. If the semantic needs to flip back to capability ("Replies"/"Replies open"), it's a one-string change with zero structural work.

## 2026-07-26 (cont. 3) — Identity line made uniform (identitySecondary v2)

- The user sensed "something different about the font" between identity lines — correctly: v1 gave the " · Department" suffix its own styling (text.muted + weight.normal) while the name ran identity's own (text.secondary/normal on read, text.default/semibold on unread) — two grays and a weight mix inside one 12px line, reading as a subtle second font. Per explicit ask ("убери різницю — хай однакове буде") the suffix now INHERITS the identity's color and weight in every state: one uniform style per line, the middle dot alone carries the separation, row-level hierarchy stays with unread/read. `inbox.identitySecondary` keeps no tokens of its own anymore (v2 note in its $description); the `__identity-dept` span/CSS are gone from both the docs page and the prototype.
- Verified statically from the generated output (browser tool's safety classifier was temporarily down): zero identity-dept leftovers, identity spans render "Betty Locherty · Office of the Registrar" as single-style text.

## 2026-07-29 — Real photo avatars for the three recurring people

- Alexander Robinson, Betty Locherty and Ava Robinson got real portrait photos (user-provided, saved to `assets/avatars/` at 128px/~5KB each via sips). The prototype's avatarMarkup now exercises **Avatar's own photo tier** (top of the photo → initials → icon fallback chain): a name with a photo renders `<img class="avatar__img">` (object-fit cover) inside the same .avatar shell; departments and everyone else (incl. the self-bubble template) stay on the initials tier — a real mixed photo/initials list, which is exactly how the component docs say Avatar degrades. Also added the component-mandated 1px border.default hairline to the prototype's .avatar (the docs page always had it; the prototype's initials-only usage had skipped it).
- Browser verification was blocked by the harness safety-classifier outage (navigate/js tools down); verified statically instead: 17 avatar__img occurrences in the built page, asset serves 200 over the local root server, ../../assets path scheme identical to the fonts link that already works locally and on Vercel.
